// AUTO-GENERATED by Tools/FnAutogen/oafnautogen.py — DO NOT EDIT.
// Source of truth: Tools/FnAutogen/Schema/Core/CoreFnMatrixElemwise.toml.
//
// Tests for context-backed public overloads.
// The public overload test doubles as the kernel correctness test —
// it calls OaFnMatrix::<Op>(...) which dispatches the SPIR-V kernel directly.
// CPU references come from each op's `forward_op` field.

#include <gtest/gtest.h>
#include <Oa/Core/FnMatrix.h>
#include <Oa/Runtime/Engine.h>
#include <Oa/Runtime/Context.h>
#include <Oa/Runtime/ComputeGraph.h>
#include <algorithm>
#include <cmath>
#include <numeric>
#include <vector>

static OaComputeEngine* GRt = nullptr;

class TestFnMatrixElemwise : public ::testing::Test {
protected:
	static void SetUpTestSuite() {
		OaEngineConfig cfg{};
		cfg.AppName = "TestFnMatrixElemwise";
		auto r = OaComputeEngine::Create(cfg);
		ASSERT_TRUE(r.IsOk()) << r.GetStatus().GetMessage();
		static OaUniquePtr<OaComputeEngine> rt = std::move(*r);
		GRt = rt.get();
	}
};

TEST_VK(TestFnMatrixElemwise, Add_Context) {
	constexpr OaU32 N = 256;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	auto b = OaFnMatrix::Rand(OaMatrixShape{N});
	std::vector<float> b_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(b, b_host.data(), N * sizeof(float)).IsOk());
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Add(a, b);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = a_host[i] + b_host[i];
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-4f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, Sub_Context) {
	constexpr OaU32 N = 256;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	auto b = OaFnMatrix::Rand(OaMatrixShape{N});
	std::vector<float> b_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(b, b_host.data(), N * sizeof(float)).IsOk());
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Sub(a, b);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = a_host[i] - b_host[i];
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-4f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, Mul_Context) {
	constexpr OaU32 N = 256;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	auto b = OaFnMatrix::Rand(OaMatrixShape{N});
	std::vector<float> b_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(b, b_host.data(), N * sizeof(float)).IsOk());
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Mul(a, b);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = a_host[i] * b_host[i];
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-4f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, Div_Context) {
	constexpr OaU32 N = 256;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	auto b = OaFnMatrix::Rand(OaMatrixShape{N});
	std::vector<float> b_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(b, b_host.data(), N * sizeof(float)).IsOk());
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Div(a, b);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = a_host[i] / b_host[i];
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-4f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, Scale_Context) {
	constexpr OaU32 N = 256;
	constexpr OaF32 kScalar = 1.5f;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Scale(a, kScalar);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = a_host[i] * kScalar;
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-4f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, Neg_Context) {
	constexpr OaU32 N = 256;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Neg(a);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = -a_host[i];
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-4f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, Abs_Context) {
	constexpr OaU32 N = 256;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Abs(a);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = std::abs(a_host[i]);
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-4f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, Log_Context) {
	constexpr OaU32 N = 256;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});  // [0, 1)
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Log(a);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = std::log(a_host[i]);
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-3f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, Sqrt_Context) {
	constexpr OaU32 N = 256;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});  // [0, 1)
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Sqrt(a);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = std::sqrt(a_host[i]);
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-3f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, Pow_Context) {
	constexpr OaU32 N = 256;
	constexpr OaF32 kScalar = 1.5f;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});  // [0, 1)
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Pow(a, kScalar);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = std::pow(a_host[i], kScalar);
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-3f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, AddScalar_Context) {
	constexpr OaU32 N = 256;
	constexpr OaF32 kScalar = 1.5f;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::AddScalar(a, kScalar);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = a_host[i] + kScalar;
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-4f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, SubScalar_Context) {
	constexpr OaU32 N = 256;
	constexpr OaF32 kScalar = 1.5f;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::SubScalar(a, kScalar);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = a_host[i] - kScalar;
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-4f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, DivScalar_Context) {
	constexpr OaU32 N = 256;
	constexpr OaF32 kScalar = 1.5f;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::DivScalar(a, kScalar);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = a_host[i] / kScalar;
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-4f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, Exp_Context) {
	constexpr OaU32 N = 256;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Exp(a);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = std::exp(a_host[i]);
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-3f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, Sin_Context) {
	constexpr OaU32 N = 256;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Sin(a);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = std::sin(a_host[i]);
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-3f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, Cos_Context) {
	constexpr OaU32 N = 256;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Cos(a);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = std::cos(a_host[i]);
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-3f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, Reciprocal_Context) {
	constexpr OaU32 N = 256;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::Reciprocal(a);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = 1.0f / a_host[i];
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-3f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, ClampMax_Context) {
	constexpr OaU32 N = 256;
	constexpr OaF32 kScalar = 0.5f;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::ClampMax(a, kScalar);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = std::min(a_host[i], kScalar);
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-4f) << "i=" << i;
}

TEST_VK(TestFnMatrixElemwise, ClampMin_Context) {
	constexpr OaU32 N = 256;
	constexpr OaF32 kScalar = -0.5f;
	auto a = OaFnMatrix::Rand(OaMatrixShape{N});
	OaContext::Scope ctx_scope(OaContext::GetDefault());
	auto out = OaFnMatrix::ClampMin(a, kScalar);
	// Context scope auto-executes and syncs at end of scope
	std::vector<float> a_host(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(a, a_host.data(), N * sizeof(float)).IsOk());
	std::vector<float> want(N);
	for (OaU32 i = 0; i < N; ++i) want[i] = std::max(a_host[i], kScalar);
	std::vector<float> got(N);
	ASSERT_TRUE(OaFnMatrix::CopyToHost(out, got.data(), N * sizeof(float)).IsOk());
	for (OaU32 i = 0; i < N; ++i) EXPECT_NEAR(got[i], want[i], 1e-4f) << "i=" << i;
}

