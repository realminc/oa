# OA ML to OARS port inventory

**Status:** Canonical active migration ledger

**Updated:** 2026-09-10

This document prevents the Rust port from independently rebuilding facilities
and kernels already implemented in OA C++. The donor repository is `oa`; paths
below are relative to its root. Rust source layout and public syntax may differ,
but numerical algorithms, graph semantics, lifecycle behavior, schemas, shader
bodies, and acceptance oracles are port inputs rather than design prompts.

## Port rule

Before implementing an ML or execution feature:

1. locate its OA operation or NN schema;
2. inspect the public contract, private implementation, Slang source, tests,
   and owning architecture document;
3. classify each imported component as `verbatim`, `mechanical adaptation`,
   `Rust redesign`, or `replacement`;
4. preserve shader math and dispatch geometry unless a replacement passes the
   donor oracle, differential comparison, and canonical benchmark gate;
5. generate all mechanically derivable Rust, Python, registry, autograd,
   documentation, and test surfaces from the ported schema;
6. remove the prototype route only after the donor-equivalent route is proven.

Mechanical adaptation includes Slang module/import spelling, OA attributes,
bindless push-field names, and Rust ABI metadata. It does not include changing
the algorithm, reduction order, tiling, saved-state contract, dtype behavior,
or numerical edge cases. A `replacement` records why reuse is impossible and
keeps the donor route as the correctness baseline until qualification.

Every checked-in ported shader will carry its donor path in the source banner
or owning generated metadata. A new shader with no donor states that explicitly.

## Current scale and conflict

The current repositories contain:

| Surface | OA C++ | OARS | Meaning |
| --- | ---: | ---: | --- |
| ML Slang modules | 231 | 117 | OARS has a growing but still incomplete donor subset; Core Softmax, Sum, and categorical counters live with Matrix rather than being counted as ML kernels. |
| ML operation-schema rows | 224 | 119 | Ninety rows are semantic operations; twenty-nine are private lowering-only kernels. The Rust schema is not yet the complete OA schema port. |
| ML public headers / Rust implementation files | 74 | 82 | Counts are directional, not API equivalence. |
| ML C++ / Rust test files | 55 | 29 | Most donor oracle packs are not ported. |
| ML tutorial translation units | 31 | 2 | Char RNN and Transformer are only the first rows. |

All 119 current OARS ML schema entries overlap an existing OA operation or
lowering family. Ninety are semantic operations; twenty-nine are explicitly
lowering-only donor kernels used by optimizer and fused inference routes. Most earlier
semantic rows were independently authored for the first Rust tutorial
instead of ported from the donor. They remain Experimental compatibility
baselines, not the canonical final implementations.

## Execution and training substrate

| OA contract and donor | OARS state | Port decision |
| --- | --- | --- |
| Canonical semantic SSA graph: `source/cpp/include/oa/core/op.h`, `source/cpp/include/oa/runtime/semanticGraph.h`, `source/cpp/lib/oa/runtime/semanticGraph.cpp` | Partial connected port: typed operation contracts and a Vulkan-free graph preserve values, views, attributes, effects, aliases, mutation, control, and autograd ranges. Matrix and current ML schemas generate compatibility identities; capture records their values, semantic attributes, metadata views, many-to-many executable provenance, and reached `GradientTape` forward/backward ranges. Recording generations prevent a submitted or aborted graph from accepting a stale tape completion. Private AdamW mutation emits fresh semantic state/parameter/moment values aliasing their prior versions while retaining the same physical storage binding, so captured optimizer dispatches are schema-owned rather than compatibility nodes. The deterministic `oa.semantic_graph.v2` diagnostic report is connected. Persisted descriptor serialization and exact OA schema identities remain missing. | Port OA schema rows and hashes progressively. Keep the current narrower OARS hashes explicitly distinct until behavior matches the donor contract, and extend functionalized mutation only with each admitted in-place writer. |
| Private mutable execution owner: `source/cpp/lib/oa/runtime/executionSession*.cpp` | Partial semantic recording now extends the existing eager owner. Matrix identities, producer reuse, metadata-view lineage, transactional recording, and compatibility-node distinction exist. | Continue the existing Rust owner; do not add another context or public runtime facade. Port stable-resource frames, complete domain bindings, stats, and failure recovery. |
| Immutable compiled plan: `source/cpp/include/oa/runtime/executionPlan.h`, `source/cpp/lib/oa/runtime/executionPlan*.cpp` | Partial compute replay plus a retained semantic graph, validated many-to-many lowering provenance, private DNN analysis/replacement, exact events, barriers, command caching, input rebinding/upload, timestamps, logical/physical resource counts, and ordered training compilation-stage evidence exist. Schema-generated replay roles drive pre-commit optimizer and RNG validation. The normalized handle-free `oa.execution_graph.v3` report is connected with generated kernel/dtype names and exact current hazard scopes. | Preserve and extend. Add persisted descriptor and RNG-state serialization, richer allocator evidence, multi-queue evidence, and more qualified DNN providers. |
| OaDna compatibility planner: `source/cpp/lib/oa/runtime/dnn.{h,cpp}`, `source/cpp/lib/oa/runtime/dnn/graphLowering.{h,cpp}`, generated `dnnOpRoles.inc` | Partial private port: schema-generated OARS compatibility roles feed a deterministic semantic-graph analyzer with portable, QKV, gated-FFN, residual-norm, attention, grouped-MoE, BLAS epilogue, and vision-preprocess partition vocabulary. Physical providers mechanically port the exact `[1024,32]` FP32 inference QKV projection+bias replacement and the exact `M=1024, N=64, K=32` Linear+Linear+SwiGLU gate/up replacement. Both preserve many-to-one semantic ownership and report applied/inherited/fallback state; gate/up additionally proves its eliminated intermediates have no external consumer or owner. Training and every unqualified candidate retain source execution with an explicit reason. | Port exact OA identities and remaining providers as their complete behavior lands, attach mutation provenance, and keep provider policy private. |
| Stable-resource frames and transient alias materialization in `ExecutionSession` and `TrainingProgram` | Connected Experimental slice: the engine-owned session reuses exact-size Matrix storage by allocation ordinal; `ItTraining::seal_replay_inputs` separates the stable external prefix from capture-local temporaries after one eager warm-up, and automatic training capture invokes that boundary after every preparation. Captured plans retain deterministic resource IDs, byte sizes, first/last executable-node access, semantic storage bindings, external-value state, explicit observed training loss, and fail-closed `Rc` retained-owner accounting. Qualified disjoint intervals are greedily grouped and rebound transactionally to one private Vulkan buffer arena; replaced allocations bypass the general pool. Allocation or replacement-planning failure retains the original graph and records an observable fallback reason. OARS deliberately uses one buffer identity per group rather than OA C++'s distinct aliasing buffer views so the current hazard planner sees alias transitions and bindless pressure drops. | Add allocator statistics that distinguish logical savings from VMA allocation/block bytes, and run GPU-assisted plus broader odd-size/guard qualification before promotion. |
| Replay RNG: `philoxGraphAdvance.slang`, `philoxUniformGraph.slang`, `philoxNormalGraph.slang`, and `TrainingProgram::prepareReplayRng_` | Connected Experimental port: schema-owned Philox4x32-10 eager uniform/normal kernels preserve the complete 64-bit seed and range/normal parameters. Capture selects physical replay variants with graph-resident per-operation U32 counters and records one state advance after each RNG use. Validation rejects frozen RNG, missing advances, and orphan advances. Dropout forward/backward use synchronized independent counters and regenerate the same effective-seed mask. | Preserve the Rust recording-time transformation, which fixes the donor graph ABI's lost seed/range fields without post-mutating a frozen executable. Add shared streams, overflow policy, serialized RNG state, broader dtypes, and donor differential vectors. |
| `TrainingProgram`: `source/cpp/include/oa/ml/trainingProgram.h`, `source/cpp/lib/oa/ml/trainingProgram.cpp` | Narrow fixed-shape forward/backward/AdamW/Dropout capture exists. Automatic training capture snapshots rather than consumes the source recording, validates schema-classified replay roles, pre-records the reusable untimed Vulkan command, commits only after those stages succeed, and leaves exact eager work available on rejection. Its first replay is therefore a command-cache hit. Successful programs expose the donor-ordered eleven compilation stages and stage-specific counts; explicit capture reports lazy command recording as not run. Latest-replay completion query/wait and consuming wait-and-release reset are connected. Deterministic `oa.training_compilation.v2`, `oa.semantic_graph.v2`, and `oa.execution_graph.v3` JSON reports cover compiler, semantic, lowering, memory, executable, hazard, and replay evidence. Training capture deliberately preserves source lowering and reports inference-only multi-operation candidates as fallbacks. | Treat current code as a compatibility slice. Port failed-stage reports, additional OaDna providers, and persisted descriptor/RNG-state serialization. |
| `ItTraining`: `source/cpp/include/oa/ml/itTraining.h`, `source/cpp/lib/oa/ml/itTraining.cpp` | Partial connected `ItTraining` port: fixed/variable epoch lifecycle, eager and existing-program completion, checked workload/loss accounting, exact GPU timing distributions, donor-style progress/summary callbacks, explicit `finish(self)`, and conservative post-warm-up stable frames exist. `new_eager` accepts the object-safe `Optimizer` behavior contract, including `NoOpOptimizer`, SGD, Adam, and Muon; `new_eager_checkpointable` admits exact native persistence. The concise `new` route retains AdamW-only whole-program capture. The automatic `step(prepare, record)` path performs one eager warm-up, captures the next fixed-shape step, skips graph authoring during replay, rejects executable preparation work, and supports explicit safe recapture. Plan rejection submits the preserved source step eagerly, disables repeated capture attempts, and exposes cumulative fallback count/latest reason; recapture re-enables compilation without erasing that evidence. Untimed replay retains one-command caching, and the retained program exposes all three donor diagnostic reports. | Port reusable training-timestamp recording, phase distributions, and optimizer-specific captured-program capabilities, including device-state Muon. |
| Metrics: `metric.h/.cpp` and `mlFnMetric.toml` | Partial connected port: object-safe completed-step metrics and mean/last `LossMetric` are sampled once after exact completion. Core Matrix schema rows now own device-resident categorical and masked-categorical U32 counters for U8/U32/I32 labels; public `ml::metric::scalar_loss` and `ml::metric::accuracy` make the blocking scalar host boundary explicit. NLP accuracy uses this route and reads one scalar rather than the complete logits and target tensors. | Add a stateful categorical metric only when the training iterator can pass predictions and labels once per completed batch; do not add per-callback reads or another execution owner. |
| Callbacks and schedules: `callbacks.h/.cpp`, `callback/lrScheduler.cpp` | Partial connected port: borrowed object-safe callbacks receive immutable snapshots in registration order and return explicit continue/stop or errors. Their restricted context exposes `dyn Optimizer`, while a sealed capability admits exact checkpoint owners. Progress/summary/CSV reporting, validation, checkpoint/restore-best, phase transitions, early stopping, and the complete donor learning-rate schedule family share this lifecycle. Validation exposes one shared completed result and excludes evaluator wall time; checkpoint I/O is likewise excluded. Phase totals are checked against the iterator schedule. A cooperative stop no longer suppresses later callbacks at the same reached boundary. CSV uses bounded buffering and explicit fallible flush boundaries. | Port remaining donor callbacks with their real dependencies; do not widen the restricted callback context into re-entrant loop access. |
| Live control: `trainingSession.h/.cpp`, `oaTrainingControl.md` | Connected Experimental port: a cloneable thread-safe `TrainingSession` owns bounded commands/results/snapshots while the non-`Send` iterator applies commands only at pre-step safe points. Pause/resume/stop, blocking wakeup, checkpoint/evaluate/rebuild handlers, optimistic revisions, hot/recapture/rebuild/immutable typed parameters, recapture validation, named scalar publication, terminal states, and independent result observers are covered by a hardware-backed donor-equivalent test. Rust attachment is intentionally `ItTraining::attach_session` rather than a mutable raw session back-pointer. | Add transport adapters only as consumers of this handle; add persisted control descriptors and broader captured-program recapture once their owning formats exist. |
| Registration-addressed model and optimizer checkpoint; `modelFile.h/.cpp` | Connected Experimental native `.oam` v3 codec replaces the incompatible `OARSML01` stream. Parameters, persistent buffers, Adam/AdamW moments, exact no-momentum SGD state, Muon momentum, and progress use donor sections and ordering. FNV section/manifest integrity, exact sizing, atomic replacement, v1/v2 reads, `CheckpointCorrupt` failures, best/latest policy, bounded rotation, and restore-best are implemented. Hardware tests prove persistent/non-persistent behavior, optimizer restoration, callback restoration, and bidirectional OA C++ `modelctl` parsing. | Expose a general-purpose `oa::ml::ModelFile` only with its full semantic API; add Q4/Q8 restoration, translators, richer config/progress, and persisted descriptor/RNG state without creating a second format. |
| Environment metadata and transition contract: `environment.h/.cpp` | Connected Experimental value layer: checked Box, Discrete, and Binary spaces define batched Matrix metadata; `EnvironmentSpec` preserves separate observation, action, reward, terminated, and truncated contracts; a hardware test proves a complete CartPole-shaped transition without readback in validation. | Port the donor execution lifecycle through the existing Engine/session boundary before rollout storage, policies, or algorithms. Do not create a second runtime or RL tensor type. |

## Current shader overlap

| OARS family | OA donor authority | Required action |
| --- | --- | --- |
| `linear`, input backward, parameter backward | routed Core GEMM/BlasLt plus `biasAdd.slang`, `linearDataBwd.slang`, and `linearWeightBiasBwd*.slang` | The semantic route now preserves OA's arbitrary leading dimensions by flattening `[..., I]` to GEMM rows and restoring `[..., O]`; the tiled donor data adjoint and row-parallel parameter adjoint cover that flattened view. Port the routed Core GEMM/BlasLt provider family and its remaining specialized backward candidates. The direct forward and narrow parameter shaders remain temporary baselines. |
| Cross-entropy, masked cross-entropy, and backward | `crossEntropy*.slang`, `maskedCrossEntropy*.slang`, `mlFnLoss.toml`, and `mlFnLossCore.toml` | Donor numerical order, stable log-sum-exp, FP32 mean reduction, U32/non-negative I32 target behavior, mask-zero behavior, selected-row normalization, bounds-safe NaN behavior, schema identity, generated attachments, and independent forward/backward tests are connected. UInt8 targets and BF16 logits remain gated on broader Rust scalar storage; the fused loss+gradient lowering remains unported. |
| Smooth L1, MSE, L1, BCE, and backward | `mlFnLossCore.toml`, `mlFnLossExtensions.toml`, `fnLoss.cpp`, `sdk/cpp/lib/ml/fnLoss.cpp`, and the loss shaders under `source/cpp/lib/oa/ml/shader/compute/loss` | Mechanically adapted donor FP32 formulas, schema identities, generated attachments, detached-target prediction adjoints, validation, and independent Vulkan oracles are connected. Smooth L1 selects the donor fused scalar-mean candidate and therefore records one physical dispatch; MSE, L1, and BCE retain the donor per-element plus Sum/Scale route. Each family owns self-identifying shader basenames below `src/slang/ml/loss/<family>/`; broader dtypes remain gated. |
| Core Softmax, LogSoftmax, Sum, categorical counters, and adjoints | Core `coreFnMatrixReduce.toml`, `fnMatrixReduce.cpp`, `autogradReduce.h`, `softmax*.slang`, `logSoftmax*.slang`, `sum.slang`, `sumAxis.slang`, `categoricalAccuracyCount.slang`, and `maskedCategoricalAccuracyCount.slang` | `matrix_reduce.json` owns public axis-aware `matrix::{softmax, log_softmax, sum}`, their private adjoints, and device-resident categorical count operations with semantic contracts, embedded artifacts, and generated oracles. LogSoftmax preserves donor stable IDs 270/271 and the barrier-safe ping-pong reduction order. The counters preserve the donor one-workgroup reduction and first-maximum tie rule while sharing checked U8/U32 storage access. Full and keep-dimension axis Sum preserve the donor reduction route; reverse Sum performs the exact broadcast adjoint. Independent Vulkan finite-difference tests prove both normalization families through parameterless `nn` adapters. Remaining Core reductions are still missing. |
| AvgPool2d and input adjoint | `mlFnMatrixPool.toml`, `mlFnMatrixBackward.toml`, `mlNnPool.toml`, `avgPool2d.slang`, and `avgPool2dBwd.slang` | The mechanically adapted FP32 NCHW forward and deterministic gather-style input adjoint are connected through `ml::matrix::avg_pool_2d` and parameterless `nn::AvgPool2d`. Odd padded geometry, valid-element border divisors, semantic attributes, invalid contracts, and finite-difference gradients have independent Vulkan oracles. OARS uses IDs 272/273 because committed OARS Muon compatibility rows already occupy the donor-local 89/90 values; stable identities are never reused. Broader dtype and rectangular-kernel variants remain unported. |
| MaxPool2d, argmax, and input adjoint | `mlFnMatrixPool.toml`, `mlFnMatrixBackward.toml`, `mlNnPool.toml`, `maxPool2d.slang`, and `maxPool2dBwd.slang` | The FP32 NCHW forward, explicit U32 argmax result, first-maximum tie rule, and deterministic gather-style input adjoint are connected through `ml::matrix::max_pool_2d` and parameterless `nn::MaxPool2d`. OARS corrects the donor shader's mismatch between its declared UInt32 result and float-encoded stored index by using `store_u32`/`load_u32`; donor-vector values, exact indices, and gradients pass on Vulkan. IDs 274/275 avoid the same committed OARS identity collision. Broader dtype and rectangular-kernel variants remain unported. |
| AdaptiveAvgPool2d and input adjoint | generated `mlNnVisionAdvanced.toml`, `adaptiveAvgPool2d.cpp`, and the older NN oracle pack | Recorded replacement for an inconsistent donor row: the generated class accepts one square extent, historical tests call two extents, and the implementation derives width geometry from height before invoking square AvgPool2d. `ml::matrix::adaptive_avg_pool_2d` and `nn::AdaptiveAvgPool2d` implement independent floor/ceiling bins for exact rectangular and non-divisible outputs. A `3x4 -> 2x3` Vulkan oracle proves forward values and the deterministic finite-difference adjoint. IDs 276/277 are OARS compatibility identities. |
| BatchNorm2d, running state, and adjoints | generated `mlNnVisionAdvanced.toml`, `batchNorm2d.cpp`, `fnMatrixBackward.cpp`, and the donor stats/normalization shaders | Connected recorded replacement: `ml::matrix` owns training-with-saved-statistics and fixed-statistics forms; `nn::BatchNorm2d` owns affine parameters plus persistent running mean/variance and obeys recursive train/eval mode. OARS uses centered population variance instead of cancellation-prone `E[x^2]-E[x]^2`, and its inference input adjoint treats running state as fixed instead of ignoring the donor mode flag. Four Intel Vulkan tests prove forward values, large-constant finiteness, EMA state, persistence metadata, train/eval behavior, and training/eval finite-difference adjoints. IDs 278-282 are OARS compatibility identities. |
| Upsample nearest/bilinear and input adjoints | `mlNnVision.toml`, `mlFnMatrixBackward.toml`, `visionFnKernelIdentity.toml`, `upsample.cpp`, and the four donor resize shaders | Connected FP32 NCHW port: `ml::matrix::upsample_2d` owns one semantic operation with nearest and align-corners-false half-pixel bilinear physical candidates; `nn::Upsample` preserves bilinear default and explicit mode selection. The bilinear adjoint replaces donor float-atomic scatter with an equivalent deterministic gather, including duplicated clamped-edge contributions. Independent forward and finite-difference adjoint oracles pass for both modes on Intel Vulkan. IDs 283-286 are OARS compatibility identities. |
| grouped Conv2d and all adjoints | `mlNnConv.toml`, `mlFnMatrixConv.toml`, `fnMatrixModules.cpp`, `autogradConv.h`, and `nn/conv/conv2d*.slang` | Connected FP32 NCHW port: `ml::matrix::conv_2d` owns the grouped OIHW forward contract and `nn::Conv2d` owns deterministic weight/bias parameters. One structured backward semantic operation lowers to separate deterministic input- and parameter-adjoint dispatches. OARS uses signed intermediate coordinates to remove donor unsigned underflow and repairs the donor parameter shader's invalid bias identifiers. Independent grouped forward and depthwise finite-difference input/weight/bias oracles pass on Intel Vulkan. IDs 287-289 are OARS compatibility identities. |
| Conv1d im2col/GEMM and all adjoints | `mlNnConv.toml`, `mlFnMatrixConv.toml`, `fnMatrixConv1dGemm.cpp`, `autogradConv.h`, `im2Col1d.slang`, `biasAdd.slang`, `transposeBatched.slang`, and `conv1dBwd*.slang` | Connected FP32 NCL port: one `ml::matrix::conv_1d` semantic operation lowers through the donor im2col layout, existing 64x64x16 tiled MatMulNt, in-place last-axis bias broadcast, and tiled batched transpose. `nn::Conv1d` owns deterministic OIK weight/bias parameters and preserves stride, padding, and dilation. One structured backward uses the donor's retained deterministic explicit input/weight/bias adjoint kernels. Independent dilated, padded, strided forward and finite-difference adjoint oracles pass on Intel Vulkan. IDs 290-294 are OARS compatibility identities. Port the donor's newer compositional GEMM backward before claiming wide-convolution training-performance parity. |
| ConvTranspose1d and adjoints | `mlNnConv.toml`, `mlFnMatrixConv.toml`, `convTranspose1d.cpp`, `autogradConv.h`, `conv1dBwdData.slang`, `im2Col1d.slang`, and `conv1dBwdWeight.slang` | Connected bias-free FP32 NCL port: `ml::matrix::conv_transpose_1d` preserves explicit dilation while `nn::ConvTranspose1d` preserves the donor's fixed-unit-dilation IOK module contract. Forward is the Conv1d data adjoint. One structured backward reuses Conv1d im2col, the existing 64x64x16 tiled MatMulNt, batched transpose, and deterministic parameter adjoint rather than introducing another convolution implementation. Independent dilated forward and finite-difference input/weight oracles pass on Intel Vulkan. IDs 295-296 are OARS compatibility identities. |
| ConvTranspose2d and adjoints | `mlNnConv.toml`, `mlFnMatrixConv.toml`, `fnMatrixModules.cpp`, `convTranspose2d.cpp`, `autogradConv.h`, `conv2dBwdData.slang`, and `convTranspose2dBwdWeight.slang` | Implemented FP32 NCHW port: `ml::matrix::conv_transpose_2d` and `nn::ConvTranspose2d` preserve IOKK weights, output-channel bias, stride, padding, data-adjoint forward, and deterministic input/weight/bias adjoints. One semantic forward lowers to data-adjoint plus bias dispatches; one semantic backward lowers to input plus parameter dispatches. Signed coordinates remove donor unsigned underflow. IDs 305-308 are OARS compatibility identities. Independent forward and finite-difference tests are committed, but the current environment returned `ERROR_INITIALIZATION_FAILED` before Vulkan device enumeration, so hardware qualification remains pending. |
| GRU cell/whole-sequence scan and adjoints | `mlFnMatrixRecurrent.toml`, `mlNnRecurrent.toml`, `gru.cpp`, `autogradRecurrent.h`, `autogradGruScan.cpp`, `gruCellLinear.slang`, `gruCellPointwiseBwd.slang`, `gruScan.slang`, and `gruScanBwd.slang` | Implemented FP32 port: `nn::GruCell` consumes caller-owned `[B,H]` state, while `nn::Gru` hoists each layer's recurrence-free input projection into the existing batched Linear route and records one recurrent scan dispatch. Cell backward composes the donor pointwise adjoint, existing Linear data/parameter adjoints, and hidden-gradient addition as one semantic operation. Scan backward records one BPTT scan plus the existing Linear parameter-adjoint dispatch. Reset/update/candidate order, zero initial state, optional paired biases, `[3H,I]`/`[3H,H]` layouts, saved state, and the 1024-hidden groupshared limit match the donor. IDs 311-314 are OARS compatibility identities. Independent cell/scan forward, finite-difference parameter, stacked, bias-free, frozen-bias, and invalid-contract tests are present; hardware qualification is pending because the current environment cannot enumerate a Vulkan device. |
| eager and graph AdamW | `adamw.slang`, `adamwGraph.slang`, `adamwGraphAdvance.slang`, `adamwMany4.slang`, `adamwMany4Graph.slang`, and `mlFnMatrixOptim.toml` | Mechanically adapted for FP32 with the donor's numerical order and four-parameter eager/replay batching. Each batched physical node retains four ordinary AdamW semantic owners; one-to-four tails use the scalar donor path. Broader dtype and optimizer variants remain missing. |
| SGD and momentum SGD | `sgd.slang`, `sgdMomentum.slang`, `sgd.cpp`, and `mlFnMatrixOptim.toml` | Mechanically adapted FP32 schemas, shaders, lowering, stable parameter state, optional momentum ownership, and `ItTraining::new_eager` integration are implemented. An independent Intel Vulkan hardware test covers the donor update order. Exact no-momentum native persistence is connected; live momentum persistence fails closed because `.oam` v3 has no SGD momentum payload. Captured replay is not claimed. |
| eager Adam | `adam.slang`, `adam.cpp`, and `mlFnMatrixOptim.toml` | Mechanically adapted FP32 schema, shader, lowering, stable first/second-moment ownership, eager iterator integration, and native persistence are implemented. An independent Intel Vulkan hardware test proves first-step bias correction and a fresh-owner round trip proves optimizer restoration. Captured replay is not claimed. |
| global gradient-norm clipping | `fnOptim.h/.cpp`, `clipGradNormReduce.slang`, `clipGradNormScale.slang`, and `mlFnKernelIdentity.toml` | The donor two-pass FP32 reduction/scaling route is connected through `ml::optim::clip_grad_norm`. It skips empty gradients, admits at most sixteen nonempty same-engine matrices, carries the threshold through the donor fixed parameter-buffer ABI, mutates gradients without host observation, and records one variadic semantic operation with aligned aliases over two executable nodes. Independent Vulkan tests cover exact global scaling, the no-op boundary, invalid collections, and ten-input capture. Broader dtypes and automatic iterator policy remain unported. |
| Muon | `muon.cpp`, `fnOptim.cpp`, `muonNesterov.slang`, `muonNormalize.slang`, `muonMatMulAxpby.slang`, `muonLinearCombination.slang`, `muonApply.slang`, and `muonVector.slang` | Mechanically adapted FP32 Nesterov, NS5, orientation, Moonshot-scale, vector fallback, and one-moment native persistence are implemented as one semantic optimizer operation with split executable provenance. Independent Intel Vulkan tests cover matrix/vector, tall, both-dimensions-large, and resumed-next-step behavior. | Port graph-resident scalar/step state before admitting Muon to captured training; broader dtypes remain gated. |
| Philox uniform/normal and Dropout | Core `philox.slang`, eager/graph RNG kernels, `fnMatrixRng.cpp`, Matrix RNG/composite schemas, and ML `Dropout` | Philox math is mechanically adapted with a corrected replay ABI. Inverted Dropout is one physical pass rather than the donor composite's random/comparison/scale intermediates; backward regenerates the exact mask. Eager, replay, independent-gradient, and train/eval module tests pass on Vulkan. Broader dtype and random-operation coverage remain missing. |
| Identity, Flatten, and Sequential | `mlNnUtility.toml`, `mlNnContainer.toml`, and generated donor implementations | Rust-native modules preserve identity, zero-copy differentiable flatten views, ordered child composition, deterministic `layer_N` registration, explicit names, recursive parameter paths, and train/eval propagation. Invalid Flatten dimensions return typed errors in place of donor assertions. |
| embedding and scatter adjoint | `ops/gather.slang`, `nn/gatherBwd.slang`, generated embed operation schema and tests | Port UInt8/UInt32 behavior and retain the Rust shape-preserving public improvement as an explicit API adaptation. |
| SiLU, ReLU, tanh, sigmoid, Leaky ReLU, ELU, Mish, Softplus, GELU, and backward | activation sources below `nn/activation`, ReLU/SiLU family sources, generated variants, `mlFnMatrixActivation.toml`, and `mlFnMatrixBackward.toml` | Mechanically adapted FP32 schema, Slang, public operation, private lowering, generated attachment, reverse node, and finite-difference oracle paths are connected. Parameterless donor `Relu`, `Gelu`, and `Silu` module adapters share those operation routes. Leaky ReLU saves the input required by the donor backward shader rather than reproducing the donor schema's stale output annotation. Broader dtypes remain missing. |
| two-input SwiGLU and gate/up lowering | `swiglu.slang`, `swigluBwd.slang`, `gateUpSwigluBias.slang`, activation schema, and `graphLowering.cpp` | Mechanically ported for F32 with a finite-difference adjoint oracle and the exact donor-qualified inference replacement. Broader shapes retain the source nodes. |
| SwiGLU MLP module | generated donor `Swiglu` from `mlNnLinear.toml` | `nn::Swiglu` owns the exact three direct weights and optional three direct biases, preserves arbitrary leading dimensions, and differentiates both layouts. Biasless mode reuses Linear lowering with private zero bias values while exposing no fake trainable parameter. |
| RMSNorm, backward, and SwiGLU FFN | `rmsNorm.slang`, `rmsNormBwd.slang`, `Ffn`, and normalization/module schemas | The donor forward and input adjoint are mechanically adapted for arbitrary leading dimensions. Its per-row weight contributions now lower through the schema-owned Core Sum-axis kernel; the temporary ML-only reducer and stable kernel ID 115 are retired. `nn::Ffn` composes registered `norm`, `gate`, `up`, and `down` children through the existing semantic operations and proves all seven parameter gradients on Vulkan. |
| RoPE and backward | `ropeApply.slang`, `ropeApplyBwd.slang`, `autogradRope.h`, `Rope`, and kernel-identity schema | Mechanically adapted split-half Llama rotation, nonzero position offsets, and exact transpose adjoint are connected through `ml::matrix::rope` and the parameterless `nn::Rope`. Independent host forward and finite-difference reverse oracles pass on Intel Vulkan; Rust rejects odd head dimensions instead of leaving half a row unwritten. |
| Transformer model composition | `NnTransformer`, `TransformerBlock`, Embedding, LayerNorm, Linear, and attention modules | `nn::Transformer` owns the donor token/position embeddings, `block_N` stack, final normalization, and vocabulary head through one registry. `TransformerBlock` exposes causal/bidirectional visibility, masked forward, and checked runtime sequence-length changes through its one registered attention child. A two-layer Vulkan test proves the complete 38-parameter reverse path, and the character tutorial delegates to this module instead of maintaining a second composition. MoE and adaptive-conditioning block variants remain unported. |
| batched NN/NT/TN matrix multiplication | `mlFnMatrixAttention.toml`, `fnMatrixBmm.cpp`, and `ops/bmm{,Nt,Tn}{,Tiled16}.slang` | The three semantic F32 operations are connected through six mechanically adapted donor kernels. Private lowering preserves OA's generic/tiled threshold and records tiled providers against their semantic owner. Reverse mode composes the other two layouts exactly as the donor does. Independent generic, odd tiled, dispatch-selection, invalid-contract, and finite-difference tests pass on Intel Iris Xe, Mesa 26.2.2, Vulkan 1.4.354. IDs 317-322 are OARS compatibility identities because committed OARS rows already occupy the donor-local numbers. See [batched matrix multiplication](../ml/oaBmm.md). |
| LayerNorm and backward | `layerNorm.slang`, `layerNormN32.slang`, `layerNormBwd.slang`, normalization schemas and tests | The current generic saved-state route now owns one structured backward semantic operation: its first kernel emits data and per-row weight adjoints, then two Core Sum-axis dispatches produce weight and bias gradients. The duplicate parameter-backward operation and stable ID 39 are retired. Port the donor generic and narrow-row schedules before treating the current tutorial baseline as universal. |
| Elman RNN and BPTT | `rnnCellLinear.slang`, `rnnCellPointwise*.slang`, `rnnScan.slang`, `rnnScanBwd.slang`, recurrent schema/tests | The sequence route mechanically preserves the donor's hoisted canonical Linear input projection, one-workgroup-per-batch scan, saved previous-hidden state, optional bias, and one-dispatch BPTT. Stable IDs 29/30 name `rnn_scan`/`rnn_scan_backward`; IDs 315/316 own the explicit-state fused cell and its structured adjoint. The temporary combined Rust kernels/lowering are removed. Generator, compile, independent sequence/cell oracles, complete BPTT/cell finite differences, and stacked/frozen-bias/bias-free hardware gates pass on Intel Iris Xe, Mesa 26.2.2, Vulkan 1.4.354. |
| scaled dot-product attention and backward | `fnMatrixAttention.cpp`, `fnMatrixFlashAttention.cpp`, `multiHeadAttention.cpp`, `mlFnMatrixAttention.toml`, head-transform, BMM, scaled-masked Softmax, Dropout, and FlashAttention sources/tests | The donor standard and causal Flash providers are connected under one SDPA semantic identity. Standard execution owns split/merge heads, BMM NN/NT/TN, generic/narrow scaled-masked Softmax, optional additive mask, implicit causal visibility, saved probabilities, and a composed Q/K/V adjoint. Flash retains only row-wise FP32 log-sum-exp and records deterministic Q and K/V adjoint dispatches without atomics. `nn::MultiHeadAttention` exposes explicit causal/bidirectional visibility and Auto/Standard/Flash policy; Auto deliberately remains Standard under current Intel evidence, arbitrary masks remain Standard-only, bias-free projections expose no fake trainable parameters, and TransformerBlock propagates visibility to the same child. Training attention dropout composes the existing replay-safe Dropout operation between Softmax and BMM and caches immutable geometry masks, while evaluation restores the ordinary SDPA route. The temporary monolithic packed causal kernels are retired. Independent host, differential, finite-difference, graph-provenance, optional-input, invalid-contract, module-policy, captured dropout/reverse, and Intel Vulkan tests pass. See [scaled dot-product attention](../ml/oaAttention.md). |
| selected MoE route normalization and adjoint | `mlFnMatrixMoe.toml`, `fnMatrixMoe.cpp`, `autogradMoe.h`, `moeRouteWeights.slang`, and `moeRouteWeightsBwd.slang` | `ml::matrix::moe_route_weights` mechanically preserves F32 `[T,E]` probability plus detached I32 `[T,K]` routing, invalid-index behavior, selected-route normalization, and the exact dense probability adjoint. Schema identity, physical write ownership, semantic capture, donor-vector, invalid-contract, and finite-difference Vulkan tests are connected. Top-k, stable expert packing, grouped projections, combine, balancing, and the `nn::Moe` owner remain unported. |

OA shader paths above are rooted under
`source/cpp/lib/oa/ml/shader/compute`; Core GEMM and common storage helpers are
under `source/cpp/lib/oa/core/shader/compute`.

## Dependency order

The active port proceeds in this order:

1. introduce provenance fields and donor-drift checks in the OARS operation
   generator;
2. connect the structural semantic-graph port to engine capture and port the
   generated operation-role vocabulary;
3. extend the existing execution session with semantic bindings and stable
   resource frames;
4. port OaDna/DNN planning and lowering into the existing execution plan;
5. extend the connected replay RNG, transient alias planning, compilation
   reports, and plan reset/wait behavior with serialization and broader
   qualification;
6. replace the remaining semantic prototype shader entries family-by-family with donor-backed
   schema rows, sources, and oracle packs;
7. port `ItTraining`, metrics, callbacks, and checkpoint/evaluation lifecycle;
8. extend the connected `TrainingSession`, then port the remaining NN modules
   and NLP/RL/tutorial matrix in dependency order.

Until steps 1–5 land, new model families and tutorial-specific kernels are
paused. The existing Char RNN and Transformer gates remain regression oracles
for progressive migration; they are not justification for a parallel Rust ML
framework.
