//! Video - timestamped image/buffer resource plus format, layout, readiness, and producer lifetime

use crate::error::Result;

/// Video represents video frame resources with timing and format semantics
pub struct Video {
    // TODO: Add frame resource, format, layout, readiness, producer lifetime
}

impl Video {
    /// Create a new video frame
    pub fn new() -> Result<Self> {
        todo!("Video::new")
    }
}
