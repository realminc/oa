// @generated by tools/gen/fn/generate.py; DO NOT EDIT.
// schema=matrix_elemwise.json schema_version=1 generator_version=52
// schema_sha256=0099910a1b1fed2e756d57ed7fbe60abc9cb191c511a24fe532a0c3d28f95b2d

/// Add two matrices elementwise using multidirectional broadcasting.
///
/// Shapes use multidirectional broadcasting over at most eight axes.
///
/// The I32 route uses two's-complement wrapping arithmetic.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn add(left: &Matrix, right: &Matrix) -> Result<Matrix> {
	binary(
		left,
		right,
		&[
			(DType::F32, KernelId::MatrixAddF32),
			(DType::I32, KernelId::MatrixAddI32),
		],
		crate::core::operation::matrix::ADD,
	)
}

/// Subtract two FP32 matrices elementwise using multidirectional broadcasting.
///
/// Shapes use multidirectional broadcasting over at most eight axes.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn sub(left: &Matrix, right: &Matrix) -> Result<Matrix> {
	binary(
		left,
		right,
		&[(DType::F32, KernelId::MatrixSubF32)],
		crate::core::operation::matrix::SUB,
	)
}

/// Multiply two FP32 matrices elementwise.
///
/// Shapes use multidirectional broadcasting over at most eight axes.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn mul(left: &Matrix, right: &Matrix) -> Result<Matrix> {
	binary(
		left,
		right,
		&[(DType::F32, KernelId::MatrixMulF32)],
		crate::core::operation::matrix::MUL,
	)
}

/// Divide two FP32 matrices elementwise using multidirectional broadcasting.
///
/// Shapes use multidirectional broadcasting over at most eight axes.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn div(left: &Matrix, right: &Matrix) -> Result<Matrix> {
	binary(
		left,
		right,
		&[(DType::F32, KernelId::MatrixDivF32)],
		crate::core::operation::matrix::DIV,
	)
}

/// Multiply every FP32 matrix element by a scalar.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn scale(input: &Matrix, scalar: f32) -> Result<Matrix> {
	unary_scalar(
		input,
		scalar,
		&[(DType::F32, KernelId::MatrixScaleF32)],
		crate::core::operation::matrix::SCALE,
	)
}

/// Negate every FP32 matrix element.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn neg(input: &Matrix) -> Result<Matrix> {
	unary(
		input,
		&[(DType::F32, KernelId::MatrixNegF32)],
		crate::core::operation::matrix::NEG,
	)
}

/// Take the absolute value of every FP32 matrix element.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn abs(input: &Matrix) -> Result<Matrix> {
	unary(
		input,
		&[(DType::F32, KernelId::MatrixAbsF32)],
		crate::core::operation::matrix::ABS,
	)
}

/// Take the natural logarithm of every FP32 matrix element.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn log(input: &Matrix) -> Result<Matrix> {
	unary(
		input,
		&[(DType::F32, KernelId::MatrixLogF32)],
		crate::core::operation::matrix::LOG,
	)
}

/// Take the square root of every FP32 matrix element.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn sqrt(input: &Matrix) -> Result<Matrix> {
	unary(
		input,
		&[(DType::F32, KernelId::MatrixSqrtF32)],
		crate::core::operation::matrix::SQRT,
	)
}

/// Raise every FP32 matrix element to a scalar exponent.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn pow(input: &Matrix, exponent: f32) -> Result<Matrix> {
	unary_scalar(
		input,
		exponent,
		&[(DType::F32, KernelId::MatrixPowF32)],
		crate::core::operation::matrix::POW,
	)
}

/// Add a scalar to every FP32 matrix element.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn add_scalar(input: &Matrix, scalar: f32) -> Result<Matrix> {
	unary_scalar(
		input,
		scalar,
		&[(DType::F32, KernelId::MatrixAddScalarF32)],
		crate::core::operation::matrix::ADD_SCALAR,
	)
}

/// Subtract a scalar from every FP32 matrix element.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn sub_scalar(input: &Matrix, scalar: f32) -> Result<Matrix> {
	unary_scalar(
		input,
		scalar,
		&[(DType::F32, KernelId::MatrixSubScalarF32)],
		crate::core::operation::matrix::SUB_SCALAR,
	)
}

/// Divide every FP32 matrix element by a scalar.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn div_scalar(input: &Matrix, scalar: f32) -> Result<Matrix> {
	unary_scalar(
		input,
		scalar,
		&[(DType::F32, KernelId::MatrixDivScalarF32)],
		crate::core::operation::matrix::DIV_SCALAR,
	)
}

/// Take the exponential of every FP32 matrix element.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn exp(input: &Matrix) -> Result<Matrix> {
	unary(
		input,
		&[(DType::F32, KernelId::MatrixExpF32)],
		crate::core::operation::matrix::EXP,
	)
}

/// Take the sine of every FP32 matrix element.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn sin(input: &Matrix) -> Result<Matrix> {
	unary(
		input,
		&[(DType::F32, KernelId::MatrixSinF32)],
		crate::core::operation::matrix::SIN,
	)
}

/// Take the cosine of every FP32 matrix element.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn cos(input: &Matrix) -> Result<Matrix> {
	unary(
		input,
		&[(DType::F32, KernelId::MatrixCosF32)],
		crate::core::operation::matrix::COS,
	)
}

/// Take the reciprocal of every FP32 matrix element.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn reciprocal(input: &Matrix) -> Result<Matrix> {
	unary(
		input,
		&[(DType::F32, KernelId::MatrixReciprocalF32)],
		crate::core::operation::matrix::RECIPROCAL,
	)
}

/// Cap every FP32 matrix element at an upper bound.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn clamp_max(input: &Matrix, maximum: f32) -> Result<Matrix> {
	unary_scalar(
		input,
		maximum,
		&[(DType::F32, KernelId::MatrixClampMaxF32)],
		crate::core::operation::matrix::CLAMP_MAX,
	)
}

/// Raise every FP32 matrix element to a lower bound.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn clamp_min(input: &Matrix, minimum: f32) -> Result<Matrix> {
	unary_scalar(
		input,
		minimum,
		&[(DType::F32, KernelId::MatrixClampMinF32)],
		crate::core::operation::matrix::CLAMP_MIN,
	)
}

/// Deep-copy a dense matrix into independent device storage.
///
/// The call submits asynchronously and returns its output immediately.
///
/// # Errors
///
/// Returns an error when the shape, dtype, ownership, or runtime submission contract fails.
pub fn copy(input: &Matrix) -> Result<Matrix> {
	unary(
		input,
		&[
			(DType::F32, KernelId::MatrixCopyF32),
			(DType::U8, KernelId::MatrixCopyU8),
			(DType::I32, KernelId::MatrixCopyI32),
			(DType::U32, KernelId::MatrixCopyU32),
		],
		crate::core::operation::matrix::COPY,
	)
}
