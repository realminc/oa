#version 450

layout(local_size_x = 64) in;

layout(set = 0, binding = 0, std430) buffer ProbeData {
    uint values[];
} data;

void main() {
    const uint index = gl_GlobalInvocationID.x;
    data.values[index] = data.values[index] * 3u + 7u;
}
