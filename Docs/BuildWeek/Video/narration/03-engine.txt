OA tests a different idea: one GPU-first C plus plus and Python framework over Vulkan. Matrices, images, audio buffers, and video frames keep their meaning, while one engine owns device memory, kernel selection, graph execution, scheduling, and profiling. Operations stay stateless. Sessions own state. Semantic work lowers into dispatches, transfers, barriers, resource aliases, and explicit completion. Capabilities are selected at runtime, and unsupported paths fail visibly.
