// AUTO-GENERATED by tools/fnAutogen/oafnautogen.py — DO NOT EDIT.
// Source of truth: tools/fnAutogen/schema/vision/visionFnImageColor.toml.
// Regenerate via: python3 tools/fnAutogen/oafnautogen.py --live
// oa::FnImage — Color GPU operations.
// Unified API: records operations to oa::ExecutionSession.

#include <oa/core/matrix.h>
#include <oa/core/fnMatrix.h>
#include <oa/core/op.h>
#include <oa/vision/fnImage.h>
#include <oa/core/status.h>
#include <oa/core/types.h>
#include <oa/core/bufferAccess.h>
#include <oa/runtime/executionSession.h>
#include <oa/core/validation.h>

// ─── normalize (Apply per-channel normalization to an NCHW image tensor) ───

oa::Matrix oa::FnImage::normalize(const oa::Matrix& inImage, const oa::NormalizationParams& inParams) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::normalize(session.engine(), inImage, inParams);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::normalize, {&inImage}, {&out}, {oa::OpAttribute::fromFloat("mean0", inParams.mean[0]), oa::OpAttribute::fromFloat("mean1", inParams.mean[1]), oa::OpAttribute::fromFloat("mean2", inParams.mean[2]), oa::OpAttribute::fromFloat("std0", inParams.std[0]), oa::OpAttribute::fromFloat("std1", inParams.std[1]), oa::OpAttribute::fromFloat("std2", inParams.std[2])});
	if (not status.isOk()) return {};
	return out;
}
