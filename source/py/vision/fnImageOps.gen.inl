// AUTO-GENERATED by tools/gen/fn/generate.py — DO NOT EDIT.
// Source of truth: tools/gen/fn/schema/**.toml.
// Included inside the owning bind function.

	m.def("adaptiveThresholdGaussian", [](const oa::Matrix& inImage, oa::U32 inKernelSize, oa::F32 inC, oa::F32 inMaxValue, oa::F32 inSigma, oa::BorderMode inBorder) {
		return matrixPtr(oa::FnImage::adaptiveThresholdGaussian(inImage, inKernelSize, inC, inMaxValue, inSigma, inBorder));
	},
	  nb::arg("image"), nb::arg("kernelSize") = 11, nb::arg("c") = 0.0F, nb::arg("maxValue") = 1.0F, nb::arg("sigma") = 0.0F, nb::arg("border") = oa::BorderMode::Reflect101,
	  nb::rv_policy::take_ownership,
	  "Local Gaussian adaptive binary threshold.");

	m.def("adaptiveThresholdMean", [](const oa::Matrix& inImage, oa::U32 inKernelSize, oa::F32 inC, oa::F32 inMaxValue, oa::BorderMode inBorder) {
		return matrixPtr(oa::FnImage::adaptiveThresholdMean(inImage, inKernelSize, inC, inMaxValue, inBorder));
	},
	  nb::arg("image"), nb::arg("kernelSize") = 11, nb::arg("c") = 0.0F, nb::arg("maxValue") = 1.0F, nb::arg("border") = oa::BorderMode::Reflect101,
	  nb::rv_policy::take_ownership,
	  "Local mean adaptive binary threshold.");

	m.def("alphaBlend", [](const oa::Matrix& inA, const oa::Matrix& inB, oa::F32 inAlpha) {
		return matrixPtr(oa::FnImage::alphaBlend(inA, inB, inAlpha));
	},
	  nb::arg("a"), nb::arg("b"), nb::arg("alpha"),
	  nb::rv_policy::take_ownership,
	  "Scalar alpha blend of equal-shaped images.");

	m.def("averageBlur", [](const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder) {
		return matrixPtr(oa::FnImage::averageBlur(inImage, inKernelWidth, inKernelHeight, inBorder));
	},
	  nb::arg("image"), nb::arg("kernelWidth") = 3, nb::arg("kernelHeight") = 3, nb::arg("border") = oa::BorderMode::Reflect101,
	  nb::rv_policy::take_ownership,
	  "Normalized box blur lowered through the generic 2D filter.");

	m.def("bilateralFilter", [](const oa::Matrix& inImage, oa::U32 inKernelSize, oa::F32 inSigmaColor, oa::F32 inSigmaSpace, oa::BorderMode inBorder) {
		return matrixPtr(oa::FnImage::bilateralFilter(inImage, inKernelSize, inSigmaColor, inSigmaSpace, inBorder));
	},
	  nb::arg("image"), nb::arg("kernelSize") = 5, nb::arg("sigmaColor") = 0.1F, nb::arg("sigmaSpace") = 1.0F, nb::arg("border") = oa::BorderMode::Reflect101,
	  nb::rv_policy::take_ownership,
	  "Edge-preserving bilateral neighborhood filter.");

	m.def("brightnessContrast", [](const oa::Matrix& inImage, oa::F32 inBrightness, oa::F32 inContrast) {
		return matrixPtr(oa::FnImage::brightnessContrast(inImage, inBrightness, inContrast));
	},
	  nb::arg("image"), nb::arg("brightness") = 0.0F, nb::arg("contrast") = 1.0F,
	  nb::rv_policy::take_ownership,
	  "Affine brightness and contrast adjustment.");

	m.def("centerCrop", [](const oa::Matrix& inImage, oa::U32 inWidth, oa::U32 inHeight) {
		return matrixPtr(oa::FnImage::centerCrop(inImage, inWidth, inHeight));
	},
	  nb::arg("image"), nb::arg("width"), nb::arg("height"),
	  nb::rv_policy::take_ownership,
	  "Centered crop lowered through Crop.");

	m.def("channelReorder", [](const oa::Matrix& inImage, oa::U32 inChannel0, oa::U32 inChannel1, oa::U32 inChannel2, oa::U32 inChannel3) {
		return matrixPtr(oa::FnImage::channelReorder(inImage, inChannel0, inChannel1, inChannel2, inChannel3));
	},
	  nb::arg("image"), nb::arg("channel0"), nb::arg("channel1") = 1, nb::arg("channel2") = 2, nb::arg("channel3") = 3,
	  nb::rv_policy::take_ownership,
	  "Reorder one to four NCHW channels.");

	m.def("clamp", [](const oa::Matrix& inImage, oa::F32 inLow, oa::F32 inHigh) {
		return matrixPtr(oa::FnImage::clamp(inImage, inLow, inHigh));
	},
	  nb::arg("image"), nb::arg("low"), nb::arg("high"),
	  nb::rv_policy::take_ownership,
	  "Clamp image values to a scalar interval.");

	m.def("colorTwist", [](const oa::Matrix& inImage, const oa::Matrix& inTransform) {
		return matrixPtr(oa::FnImage::colorTwist(inImage, inTransform));
	},
	  nb::arg("image"), nb::arg("transform"),
	  nb::rv_policy::take_ownership,
	  "Apply a shared 3x4 RGB color transform matrix.");

	m.def("composite", [](const oa::Matrix& inA, const oa::Matrix& inB, const oa::Matrix& inMask) {
		return matrixPtr(oa::FnImage::composite(inA, inB, inMask));
	},
	  nb::arg("a"), nb::arg("b"), nb::arg("mask"),
	  nb::rv_policy::take_ownership,
	  "Per-pixel masked composite with one or C mask channels.");

	m.def("convertColor", [](const oa::Image& inImage, oa::ImageFormat inDstFormat) {
		return imagePtr(oa::FnImage::convertColor(inImage, inDstFormat));
	},
	  nb::arg("image"), nb::arg("dstFormat"),
	  nb::rv_policy::take_ownership,
	  "Convert an RGB semantic image to BGR, or BGR to RGB.");

	m.def("convolve2d", [](const oa::Matrix& inImage, const oa::Matrix& inKernel, oa::BorderMode inBorder, oa::F32 inBorderValue) {
		return matrixPtr(oa::FnImage::convolve2d(inImage, inKernel, inBorder, inBorderValue));
	},
	  nb::arg("image"), nb::arg("kernel"), nb::arg("border") = oa::BorderMode::Reflect101, nb::arg("borderValue") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Same-shape NCHW 2D cross-correlation with an odd shared kernel and explicit border semantics.");

	m.def("crop", [](const oa::Matrix& inImage, oa::U32 inX, oa::U32 inY, oa::U32 inWidth, oa::U32 inHeight) {
		return matrixPtr(oa::FnImage::crop(inImage, inX, inY, inWidth, inHeight));
	},
	  nb::arg("image"), nb::arg("x"), nb::arg("y"), nb::arg("width"), nb::arg("height"),
	  nb::rv_policy::take_ownership,
	  "Crop NCHW image: out = A[y:y+h, x:x+w].");

	m.def("dilate", [](const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
		return matrixPtr(oa::FnImage::dilate(inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue));
	},
	  nb::arg("image"), nb::arg("kernelWidth") = 3, nb::arg("kernelHeight") = 3, nb::arg("border") = oa::BorderMode::Reflect101, nb::arg("borderValue") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Rectangular NCHW dilation using the shared border contract.");

	m.def("erase", [](const oa::Matrix& inImage, oa::U32 inX, oa::U32 inY, oa::U32 inWidth, oa::U32 inHeight, oa::F32 inValue) {
		return matrixPtr(oa::FnImage::erase(inImage, inX, inY, inWidth, inHeight, inValue));
	},
	  nb::arg("image"), nb::arg("x"), nb::arg("y"), nb::arg("width"), nb::arg("height"), nb::arg("value") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Fill a rectangular region with a scalar value.");

	m.def("erode", [](const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
		return matrixPtr(oa::FnImage::erode(inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue));
	},
	  nb::arg("image"), nb::arg("kernelWidth") = 3, nb::arg("kernelHeight") = 3, nb::arg("border") = oa::BorderMode::Reflect101, nb::arg("borderValue") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Rectangular NCHW erosion using the shared border contract.");

	m.def("flip", [](const oa::Matrix& inImage, bool inHorizontal, bool inVertical) {
		return matrixPtr(oa::FnImage::flip(inImage, inHorizontal, inVertical));
	},
	  nb::arg("image"), nb::arg("horizontal") = false, nb::arg("vertical") = false,
	  nb::rv_policy::take_ownership,
	  "Flip NCHW image horizontally or vertically.");

	m.def("gammaContrast", [](const oa::Matrix& inImage, oa::F32 inGamma, oa::F32 inGain) {
		return matrixPtr(oa::FnImage::gammaContrast(inImage, inGamma, inGain));
	},
	  nb::arg("image"), nb::arg("gamma"), nb::arg("gain") = 1.0F,
	  nb::rv_policy::take_ownership,
	  "Gamma and gain adjustment.");

	m.def("gaussianBlur", [](const oa::Matrix& inImage, oa::F32 inSigma, oa::U32 inKernelSize) {
		return matrixPtr(oa::FnImage::gaussianBlur(inImage, inSigma, inKernelSize));
	},
	  nb::arg("image"), nb::arg("sigma"), nb::arg("kernelSize") = 0,
	  nb::rv_policy::take_ownership,
	  "Gaussian blur filter with configurable kernel size and sigma.");

	m.def("gaussianNoise", [](const oa::Matrix& inImage, oa::F32 inMean, oa::F32 inStddev, oa::U64 inSeed) {
		return matrixPtr(oa::FnImage::gaussianNoise(inImage, inMean, inStddev, inSeed));
	},
	  nb::arg("image"), nb::arg("mean") = 0.0F, nb::arg("stddev") = 0.01F, nb::arg("seed") = 0,
	  nb::rv_policy::take_ownership,
	  "Add deterministic GPU-native Philox Gaussian noise.");

	m.def("grayscale", [](const oa::Matrix& inImage) {
		return matrixPtr(oa::FnImage::grayscale(inImage));
	},
	  nb::arg("image"),
	  nb::rv_policy::take_ownership,
	  "Rec.709 RGB to single-channel grayscale conversion.");

	m.def("inRange", [](const oa::Matrix& inImage, oa::F32 inLow, oa::F32 inHigh, oa::F32 inTrueValue) {
		return matrixPtr(oa::FnImage::inRange(inImage, inLow, inHigh, inTrueValue));
	},
	  nb::arg("image"), nb::arg("low"), nb::arg("high"), nb::arg("trueValue") = 1.0F,
	  nb::rv_policy::take_ownership,
	  "Inclusive scalar range mask.");

	m.def("invert", [](const oa::Matrix& inImage, oa::F32 inMaxValue) {
		return matrixPtr(oa::FnImage::invert(inImage, inMaxValue));
	},
	  nb::arg("image"), nb::arg("maxValue") = 1.0F,
	  nb::rv_policy::take_ownership,
	  "Invert values around a configurable maximum.");

	m.def("laplacian", [](const oa::Matrix& inImage, oa::BorderMode inBorder) {
		return matrixPtr(oa::FnImage::laplacian(inImage, inBorder));
	},
	  nb::arg("image"), nb::arg("border") = oa::BorderMode::Reflect101,
	  nb::rv_policy::take_ownership,
	  "Signed four-neighbour discrete Laplacian lowered through the generic 2D filter.");

	m.def("medianBlur", [](const oa::Matrix& inImage, oa::U32 inKernelSize, oa::BorderMode inBorder) {
		return matrixPtr(oa::FnImage::medianBlur(inImage, inKernelSize, inBorder));
	},
	  nb::arg("image"), nb::arg("kernelSize") = 3, nb::arg("border") = oa::BorderMode::Reflect101,
	  nb::rv_policy::take_ownership,
	  "Odd-window median filter with explicit border semantics.");

	m.def("morphologyBlackHat", [](const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
		return matrixPtr(oa::FnImage::morphologyBlackHat(inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue));
	},
	  nb::arg("image"), nb::arg("kernelWidth") = 3, nb::arg("kernelHeight") = 3, nb::arg("border") = oa::BorderMode::Reflect101, nb::arg("borderValue") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Morphological closing minus input.");

	m.def("morphologyClose", [](const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
		return matrixPtr(oa::FnImage::morphologyClose(inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue));
	},
	  nb::arg("image"), nb::arg("kernelWidth") = 3, nb::arg("kernelHeight") = 3, nb::arg("border") = oa::BorderMode::Reflect101, nb::arg("borderValue") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Dilation followed by erosion through the reusable morphology primitive.");

	m.def("morphologyGradient", [](const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
		return matrixPtr(oa::FnImage::morphologyGradient(inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue));
	},
	  nb::arg("image"), nb::arg("kernelWidth") = 3, nb::arg("kernelHeight") = 3, nb::arg("border") = oa::BorderMode::Reflect101, nb::arg("borderValue") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Difference between dilation and erosion.");

	m.def("morphologyOpen", [](const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
		return matrixPtr(oa::FnImage::morphologyOpen(inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue));
	},
	  nb::arg("image"), nb::arg("kernelWidth") = 3, nb::arg("kernelHeight") = 3, nb::arg("border") = oa::BorderMode::Reflect101, nb::arg("borderValue") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Erosion followed by dilation through the reusable morphology primitive.");

	m.def("morphologyTopHat", [](const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
		return matrixPtr(oa::FnImage::morphologyTopHat(inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue));
	},
	  nb::arg("image"), nb::arg("kernelWidth") = 3, nb::arg("kernelHeight") = 3, nb::arg("border") = oa::BorderMode::Reflect101, nb::arg("borderValue") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Input minus morphological opening.");

	m.def("normalize", [](const oa::Matrix& inImage, const oa::NormalizationParams& inParams) {
		return matrixPtr(oa::FnImage::normalize(inImage, inParams));
	},
	  nb::arg("image"), nb::arg("params"),
	  nb::rv_policy::take_ownership,
	  "Apply per-channel normalization to an NCHW image tensor.");

	m.def("pad", [](const oa::Matrix& inImage, oa::U32 inLeft, oa::U32 inRight, oa::U32 inTop, oa::U32 inBottom, oa::BorderMode inBorder, oa::F32 inBorderValue) {
		return matrixPtr(oa::FnImage::pad(inImage, inLeft, inRight, inTop, inBottom, inBorder, inBorderValue));
	},
	  nb::arg("image"), nb::arg("left"), nb::arg("right"), nb::arg("top"), nb::arg("bottom"), nb::arg("border") = oa::BorderMode::Constant, nb::arg("borderValue") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Asymmetric NCHW padding with the shared border contract.");

	m.def("posterize", [](const oa::Matrix& inImage, oa::U32 inLevels, oa::F32 inLow, oa::F32 inHigh) {
		return matrixPtr(oa::FnImage::posterize(inImage, inLevels, inLow, inHigh));
	},
	  nb::arg("image"), nb::arg("levels"), nb::arg("low") = 0.0F, nb::arg("high") = 1.0F,
	  nb::rv_policy::take_ownership,
	  "Quantize an intensity interval to a fixed number of levels.");

	m.def("remap", [](const oa::Matrix& inImage, const oa::Matrix& inMap, oa::InterpolationMode inInterpolation, oa::BorderMode inBorder, oa::F32 inBorderValue) {
		return matrixPtr(oa::FnImage::remap(inImage, inMap, inInterpolation, inBorder, inBorderValue));
	},
	  nb::arg("image"), nb::arg("map"), nb::arg("interpolation") = oa::InterpolationMode::Bilinear, nb::arg("border") = oa::BorderMode::Constant, nb::arg("borderValue") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Absolute-coordinate NCHW remapping with nearest or bilinear sampling.");

	m.def("resize", [](const oa::Matrix& inImage, oa::U32 inTargetWidth, oa::U32 inTargetHeight, oa::InterpolationMode inMode) {
		return matrixPtr(oa::FnImage::resize(inImage, inTargetWidth, inTargetHeight, inMode));
	},
	  nb::arg("image"), nb::arg("targetWidth"), nb::arg("targetHeight"), nb::arg("mode") = oa::InterpolationMode::Bilinear,
	  nb::rv_policy::take_ownership,
	  "Resize an NCHW image using nearest or bilinear interpolation.");

	m.def("resizeNormalize", [](const oa::Image& inImage, oa::U32 inWidth, oa::U32 inHeight, const oa::NormalizationParams& inParams) {
		return imagePtr(oa::FnImage::resizeNormalize(inImage, inWidth, inHeight, inParams));
	},
	  nb::arg("image"), nb::arg("width"), nb::arg("height"), nb::arg("params"),
	  nb::rv_policy::take_ownership,
	  "Resize and normalize an NCHW or CHW semantic image in one dispatch.");

	m.def("rotate", [](const oa::Matrix& inImage, oa::U32 inDegrees) {
		return matrixPtr(oa::FnImage::rotate(inImage, inDegrees));
	},
	  nb::arg("image"), nb::arg("degrees"),
	  nb::rv_policy::take_ownership,
	  "Rotate NCHW image by 90/180/270 degrees.");

	m.def("saltPepperNoise", [](const oa::Matrix& inImage, oa::F32 inProbability, oa::F32 inSaltValue, oa::F32 inPepperValue, oa::U64 inSeed) {
		return matrixPtr(oa::FnImage::saltPepperNoise(inImage, inProbability, inSaltValue, inPepperValue, inSeed));
	},
	  nb::arg("image"), nb::arg("probability") = 0.01F, nb::arg("saltValue") = 1.0F, nb::arg("pepperValue") = 0.0F, nb::arg("seed") = 0,
	  nb::rv_policy::take_ownership,
	  "Apply deterministic GPU-native salt-and-pepper noise.");

	m.def("scharr", [](const oa::Matrix& inImage, oa::U32 inDx, oa::U32 inDy, oa::BorderMode inBorder) {
		return matrixPtr(oa::FnImage::scharr(inImage, inDx, inDy, inBorder));
	},
	  nb::arg("image"), nb::arg("dx") = 1, nb::arg("dy") = 0, nb::arg("border") = oa::BorderMode::Reflect101,
	  nb::rv_policy::take_ownership,
	  "Signed 3x3 Scharr x or y derivative lowered through the generic 2D filter.");

	m.def("segmentationOverlay", [](const oa::Matrix& inImage, const oa::Matrix& inMask, const oa::Matrix& inPalette, oa::F32 inAlpha) {
		return matrixPtr(oa::FnImage::segmentationOverlay(inImage, inMask, inPalette, inAlpha));
	},
	  nb::arg("image"), nb::arg("mask"), nb::arg("palette"), nb::arg("alpha") = 0.5F,
	  nb::rv_policy::take_ownership,
	  "Blend a semantic segmentation label map over an image.");

	m.def("separableConvolve2d", [](const oa::Matrix& inImage, const oa::Matrix& inKernelX, const oa::Matrix& inKernelY, oa::BorderMode inBorder, oa::F32 inBorderValue) {
		return matrixPtr(oa::FnImage::separableConvolve2d(inImage, inKernelX, inKernelY, inBorder, inBorderValue));
	},
	  nb::arg("image"), nb::arg("kernelX"), nb::arg("kernelY"), nb::arg("border") = oa::BorderMode::Reflect101, nb::arg("borderValue") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Same-shape NCHW separable cross-correlation with odd horizontal and vertical kernels.");

	m.def("sharpen", [](const oa::Matrix& inImage, oa::F32 inAmount, oa::BorderMode inBorder) {
		return matrixPtr(oa::FnImage::sharpen(inImage, inAmount, inBorder));
	},
	  nb::arg("image"), nb::arg("amount") = 1.0F, nb::arg("border") = oa::BorderMode::Reflect101,
	  nb::rv_policy::take_ownership,
	  "Parameterized 3x3 sharpening lowered through Convolve2d.");

	m.def("sobel", [](const oa::Matrix& inImage, oa::U32 inDx, oa::U32 inDy, oa::BorderMode inBorder) {
		return matrixPtr(oa::FnImage::sobel(inImage, inDx, inDy, inBorder));
	},
	  nb::arg("image"), nb::arg("dx") = 1, nb::arg("dy") = 0, nb::arg("border") = oa::BorderMode::Reflect101,
	  nb::rv_policy::take_ownership,
	  "Signed 3x3 Sobel x or y derivative lowered through the generic 2D filter.");

	m.def("solarize", [](const oa::Matrix& inImage, oa::F32 inThreshold, oa::F32 inMaxValue) {
		return matrixPtr(oa::FnImage::solarize(inImage, inThreshold, inMaxValue));
	},
	  nb::arg("image"), nb::arg("threshold") = 0.5F, nb::arg("maxValue") = 1.0F,
	  nb::rv_policy::take_ownership,
	  "Invert values above a threshold.");

	m.def("thresholdBinary", [](const oa::Matrix& inImage, oa::F32 inThreshold, oa::F32 inMaxValue) {
		return matrixPtr(oa::FnImage::thresholdBinary(inImage, inThreshold, inMaxValue));
	},
	  nb::arg("image"), nb::arg("threshold"), nb::arg("maxValue") = 1.0F,
	  nb::rv_policy::take_ownership,
	  "Global binary threshold.");

	m.def("thresholdBinaryInv", [](const oa::Matrix& inImage, oa::F32 inThreshold, oa::F32 inMaxValue) {
		return matrixPtr(oa::FnImage::thresholdBinaryInv(inImage, inThreshold, inMaxValue));
	},
	  nb::arg("image"), nb::arg("threshold"), nb::arg("maxValue") = 1.0F,
	  nb::rv_policy::take_ownership,
	  "Global inverse binary threshold.");

	m.def("thresholdToZero", [](const oa::Matrix& inImage, oa::F32 inThreshold) {
		return matrixPtr(oa::FnImage::thresholdToZero(inImage, inThreshold));
	},
	  nb::arg("image"), nb::arg("threshold"),
	  nb::rv_policy::take_ownership,
	  "Set values at or below a threshold to zero.");

	m.def("thresholdToZeroInv", [](const oa::Matrix& inImage, oa::F32 inThreshold) {
		return matrixPtr(oa::FnImage::thresholdToZeroInv(inImage, inThreshold));
	},
	  nb::arg("image"), nb::arg("threshold"),
	  nb::rv_policy::take_ownership,
	  "Set values above a threshold to zero.");

	m.def("thresholdTruncate", [](const oa::Matrix& inImage, oa::F32 inThreshold) {
		return matrixPtr(oa::FnImage::thresholdTruncate(inImage, inThreshold));
	},
	  nb::arg("image"), nb::arg("threshold"),
	  nb::rv_policy::take_ownership,
	  "Truncate values above a threshold.");

	m.def("unsharpMask", [](const oa::Matrix& inImage, oa::F32 inSigma, oa::F32 inAmount, oa::U32 inKernelSize) {
		return matrixPtr(oa::FnImage::unsharpMask(inImage, inSigma, inAmount, inKernelSize));
	},
	  nb::arg("image"), nb::arg("sigma") = 1.0F, nb::arg("amount") = 1.0F, nb::arg("kernelSize") = 0,
	  nb::rv_policy::take_ownership,
	  "Gaussian unsharp masking composed from graph-native primitives.");

	m.def("warpAffine", [](const oa::Matrix& inImage, const oa::Matrix& inTransform, oa::U32 inWidth, oa::U32 inHeight, oa::InterpolationMode inInterpolation, oa::BorderMode inBorder, oa::F32 inBorderValue) {
		return matrixPtr(oa::FnImage::warpAffine(inImage, inTransform, inWidth, inHeight, inInterpolation, inBorder, inBorderValue));
	},
	  nb::arg("image"), nb::arg("transform"), nb::arg("width"), nb::arg("height"), nb::arg("interpolation") = oa::InterpolationMode::Bilinear, nb::arg("border") = oa::BorderMode::Constant, nb::arg("borderValue") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Inverse 2x3 affine warp with explicit interpolation and border semantics.");

	m.def("warpPerspective", [](const oa::Matrix& inImage, const oa::Matrix& inTransform, oa::U32 inWidth, oa::U32 inHeight, oa::InterpolationMode inInterpolation, oa::BorderMode inBorder, oa::F32 inBorderValue) {
		return matrixPtr(oa::FnImage::warpPerspective(inImage, inTransform, inWidth, inHeight, inInterpolation, inBorder, inBorderValue));
	},
	  nb::arg("image"), nb::arg("transform"), nb::arg("width"), nb::arg("height"), nb::arg("interpolation") = oa::InterpolationMode::Bilinear, nb::arg("border") = oa::BorderMode::Constant, nb::arg("borderValue") = 0.0F,
	  nb::rv_policy::take_ownership,
	  "Inverse 3x3 perspective warp with explicit interpolation and border semantics.");
