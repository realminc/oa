# AUTO-GENERATED by tools/gen/python/generateStubs.py — DO NOT EDIT.
# Sources of truth: the live nanobind signatures and oa/_surface.py.

"""Native Python view of C++ namespace oa::FnImage"""

from typing import overload

import oa


def adaptiveThresholdGaussian(image: oa.Matrix, kernelSize: int = ..., c: float = ..., maxValue: float = ..., sigma: float = ..., border: oa.BorderMode = oa.BorderMode.Reflect101) -> oa.Matrix:
	"""Local Gaussian adaptive binary threshold."""

def adaptiveThresholdMean(image: oa.Matrix, kernelSize: int = ..., c: float = ..., maxValue: float = ..., border: oa.BorderMode = oa.BorderMode.Reflect101) -> oa.Matrix:
	"""Local mean adaptive binary threshold."""

def alphaBlend(a: oa.Matrix, b: oa.Matrix, alpha: float) -> oa.Matrix:
	"""Scalar alpha blend of equal-shaped images."""

def averageBlur(image: oa.Matrix, kernelWidth: int = ..., kernelHeight: int = ..., border: oa.BorderMode = oa.BorderMode.Reflect101) -> oa.Matrix:
	"""Normalized box blur lowered through the generic 2D filter."""

def bilateralFilter(image: oa.Matrix, kernelSize: int = ..., sigmaColor: float = ..., sigmaSpace: float = ..., border: oa.BorderMode = oa.BorderMode.Reflect101) -> oa.Matrix:
	"""Edge-preserving bilateral neighborhood filter."""

@overload
def brightnessContrast(image: oa.Matrix, brightness: float = ..., contrast: float = ...) -> oa.Matrix:
	"""Affine brightness and contrast adjustment."""

@overload
def brightnessContrast(image: oa.Image, brightness: float = ..., contrast: float = ...) -> oa.Image: ...

def centerCrop(image: oa.Matrix, width: int, height: int) -> oa.Matrix:
	"""Centered crop lowered through Crop."""

def channelReorder(image: oa.Matrix, channel0: int, channel1: int = ..., channel2: int = ..., channel3: int = ...) -> oa.Matrix:
	"""Reorder one to four NCHW channels."""

def clamp(image: oa.Matrix, low: float, high: float) -> oa.Matrix:
	"""Clamp image values to a scalar interval."""

def colorTwist(image: oa.Matrix, transform: oa.Matrix) -> oa.Matrix:
	"""Apply a shared 3x4 RGB color transform matrix."""

def composite(a: oa.Matrix, b: oa.Matrix, mask: oa.Matrix) -> oa.Matrix:
	"""Per-pixel masked composite with one or C mask channels."""

def convertColor(image: oa.Image, dstFormat: oa.ImageFormat) -> oa.Image:
	"""Convert an RGB semantic image to BGR, or BGR to RGB."""

def convolve2d(image: oa.Matrix, kernel: oa.Matrix, border: oa.BorderMode = oa.BorderMode.Reflect101, borderValue: float = ...) -> oa.Matrix:
	"""
	Same-shape NCHW 2D cross-correlation with an odd shared kernel and explicit border semantics.
	"""

def crop(image: oa.Matrix, x: int, y: int, width: int, height: int) -> oa.Matrix:
	"""Crop NCHW image: out = A[y:y+h, x:x+w]."""

def dilate(image: oa.Matrix, kernelWidth: int = ..., kernelHeight: int = ..., border: oa.BorderMode = oa.BorderMode.Reflect101, borderValue: float = ...) -> oa.Matrix:
	"""Rectangular NCHW dilation using the shared border contract."""

def erase(image: oa.Matrix, x: int, y: int, width: int, height: int, value: float = ...) -> oa.Matrix:
	"""Fill a rectangular region with a scalar value."""

def erode(image: oa.Matrix, kernelWidth: int = ..., kernelHeight: int = ..., border: oa.BorderMode = oa.BorderMode.Reflect101, borderValue: float = ...) -> oa.Matrix:
	"""Rectangular NCHW erosion using the shared border contract."""

def flip(image: oa.Matrix, horizontal: bool = ..., vertical: bool = ...) -> oa.Matrix:
	"""Flip NCHW image horizontally or vertically."""

def gammaContrast(image: oa.Matrix, gamma: float, gain: float = ...) -> oa.Matrix:
	"""Gamma and gain adjustment."""

def gaussianBlur(image: oa.Matrix, sigma: float, kernelSize: int = ...) -> oa.Matrix:
	"""Gaussian blur filter with configurable kernel size and sigma."""

def gaussianNoise(image: oa.Matrix, mean: float = ..., stddev: float = ..., seed: int = ...) -> oa.Matrix:
	"""Add deterministic GPU-native Philox Gaussian noise."""

def grayscale(image: oa.Matrix) -> oa.Matrix:
	"""Rec.709 RGB to single-channel grayscale conversion."""

def inRange(image: oa.Matrix, low: float, high: float, trueValue: float = ...) -> oa.Matrix:
	"""Inclusive scalar range mask."""

def invert(image: oa.Matrix, maxValue: float = ...) -> oa.Matrix:
	"""Invert values around a configurable maximum."""

def laplacian(image: oa.Matrix, border: oa.BorderMode = oa.BorderMode.Reflect101) -> oa.Matrix:
	"""
	Signed four-neighbour discrete Laplacian lowered through the generic 2D filter.
	"""

def medianBlur(image: oa.Matrix, kernelSize: int = ..., border: oa.BorderMode = oa.BorderMode.Reflect101) -> oa.Matrix:
	"""Odd-window median filter with explicit border semantics."""

def morphologyBlackHat(image: oa.Matrix, kernelWidth: int = ..., kernelHeight: int = ..., border: oa.BorderMode = oa.BorderMode.Reflect101, borderValue: float = ...) -> oa.Matrix:
	"""Morphological closing minus input."""

def morphologyClose(image: oa.Matrix, kernelWidth: int = ..., kernelHeight: int = ..., border: oa.BorderMode = oa.BorderMode.Reflect101, borderValue: float = ...) -> oa.Matrix:
	"""
	Dilation followed by erosion through the reusable morphology primitive.
	"""

def morphologyGradient(image: oa.Matrix, kernelWidth: int = ..., kernelHeight: int = ..., border: oa.BorderMode = oa.BorderMode.Reflect101, borderValue: float = ...) -> oa.Matrix:
	"""Difference between dilation and erosion."""

def morphologyOpen(image: oa.Matrix, kernelWidth: int = ..., kernelHeight: int = ..., border: oa.BorderMode = oa.BorderMode.Reflect101, borderValue: float = ...) -> oa.Matrix:
	"""
	Erosion followed by dilation through the reusable morphology primitive.
	"""

def morphologyTopHat(image: oa.Matrix, kernelWidth: int = ..., kernelHeight: int = ..., border: oa.BorderMode = oa.BorderMode.Reflect101, borderValue: float = ...) -> oa.Matrix:
	"""Input minus morphological opening."""

@overload
def normalize(image: oa.Matrix, params: oa.NormalizationParams) -> oa.Matrix:
	"""Apply per-channel normalization to an NCHW image tensor."""

@overload
def normalize(image: oa.Image, params: oa.NormalizationParams) -> oa.Image: ...

def pad(image: oa.Matrix, left: int, right: int, top: int, bottom: int, border: oa.BorderMode = oa.BorderMode.Constant, borderValue: float = ...) -> oa.Matrix:
	"""Asymmetric NCHW padding with the shared border contract."""

def posterize(image: oa.Matrix, levels: int, low: float = ..., high: float = ...) -> oa.Matrix:
	"""Quantize an intensity interval to a fixed number of levels."""

def remap(image: oa.Matrix, map: oa.Matrix, interpolation: oa.InterpolationMode = oa.InterpolationMode.Bilinear, border: oa.BorderMode = oa.BorderMode.Constant, borderValue: float = ...) -> oa.Matrix:
	"""Absolute-coordinate NCHW remapping with nearest or bilinear sampling."""

@overload
def resize(image: oa.Matrix, targetWidth: int, targetHeight: int, mode: oa.InterpolationMode = oa.InterpolationMode.Bilinear) -> oa.Matrix:
	"""Resize an NCHW image using nearest or bilinear interpolation."""

@overload
def resize(image: oa.Image, width: int, height: int) -> oa.Image: ...

def resizeNormalize(image: oa.Image, width: int, height: int, params: oa.NormalizationParams) -> oa.Image:
	"""Resize and normalize an NCHW or CHW semantic image in one dispatch."""

def rotate(image: oa.Matrix, degrees: int) -> oa.Matrix:
	"""Rotate NCHW image by 90/180/270 degrees."""

def saltPepperNoise(image: oa.Matrix, probability: float = ..., saltValue: float = ..., pepperValue: float = ..., seed: int = ...) -> oa.Matrix:
	"""Apply deterministic GPU-native salt-and-pepper noise."""

def scharr(image: oa.Matrix, dx: int = ..., dy: int = ..., border: oa.BorderMode = oa.BorderMode.Reflect101) -> oa.Matrix:
	"""
	Signed 3x3 Scharr x or y derivative lowered through the generic 2D filter.
	"""

def segmentationOverlay(image: oa.Matrix, mask: oa.Matrix, palette: oa.Matrix, alpha: float = ...) -> oa.Matrix:
	"""Blend a semantic segmentation label map over an image."""

def separableConvolve2d(image: oa.Matrix, kernelX: oa.Matrix, kernelY: oa.Matrix, border: oa.BorderMode = oa.BorderMode.Reflect101, borderValue: float = ...) -> oa.Matrix:
	"""
	Same-shape NCHW separable cross-correlation with odd horizontal and vertical kernels.
	"""

def sharpen(image: oa.Matrix, amount: float = ..., border: oa.BorderMode = oa.BorderMode.Reflect101) -> oa.Matrix:
	"""Parameterized 3x3 sharpening lowered through Convolve2d."""

def sobel(image: oa.Matrix, dx: int = ..., dy: int = ..., border: oa.BorderMode = oa.BorderMode.Reflect101) -> oa.Matrix:
	"""
	Signed 3x3 Sobel x or y derivative lowered through the generic 2D filter.
	"""

def solarize(image: oa.Matrix, threshold: float = ..., maxValue: float = ...) -> oa.Matrix:
	"""Invert values above a threshold."""

def thresholdBinary(image: oa.Matrix, threshold: float, maxValue: float = ...) -> oa.Matrix:
	"""Global binary threshold."""

def thresholdBinaryInv(image: oa.Matrix, threshold: float, maxValue: float = ...) -> oa.Matrix:
	"""Global inverse binary threshold."""

def thresholdToZero(image: oa.Matrix, threshold: float) -> oa.Matrix:
	"""Set values at or below a threshold to zero."""

def thresholdToZeroInv(image: oa.Matrix, threshold: float) -> oa.Matrix:
	"""Set values above a threshold to zero."""

def thresholdTruncate(image: oa.Matrix, threshold: float) -> oa.Matrix:
	"""Truncate values above a threshold."""

def unsharpMask(image: oa.Matrix, sigma: float = ..., amount: float = ..., kernelSize: int = ...) -> oa.Matrix:
	"""Gaussian unsharp masking composed from graph-native primitives."""

def warpAffine(image: oa.Matrix, transform: oa.Matrix, width: int, height: int, interpolation: oa.InterpolationMode = oa.InterpolationMode.Bilinear, border: oa.BorderMode = oa.BorderMode.Constant, borderValue: float = ...) -> oa.Matrix:
	"""
	Inverse 2x3 affine warp with explicit interpolation and border semantics.
	"""

def warpPerspective(image: oa.Matrix, transform: oa.Matrix, width: int, height: int, interpolation: oa.InterpolationMode = oa.InterpolationMode.Bilinear, border: oa.BorderMode = oa.BorderMode.Constant, borderValue: float = ...) -> oa.Matrix:
	"""
	Inverse 3x3 perspective warp with explicit interpolation and border semantics.
	"""

def decodeFile(path: object, format: oa.ImageFormat = oa.ImageFormat.Rgb) -> oa.Image: ...

def decodeMemory(data: bytes, format: oa.ImageFormat = oa.ImageFormat.Rgb) -> oa.Image: ...

def encode(image: oa.Image, codec: oa.ImageCodec, quality: int = ...) -> bytes: ...

def saveFile(path: object, image: oa.Image, quality: int = ...) -> None: ...

def canDecode(codec: oa.ImageCodec) -> bool: ...

def canEncode(codec: oa.ImageCodec) -> bool: ...
