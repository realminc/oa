# AUTO-GENERATED by tools/python/oapystubgen.py — DO NOT EDIT.
# Sources of truth: the live nanobind signatures and oa/_surface.py.

"""Native Python view of C++ namespace oa::FnMatrix"""

from collections.abc import Sequence
from typing import overload

import oa
import oa._oa.FnMatrix
from oa._oa.FnMatrix import (
	ChannelNormBwdResult as ChannelNormBwdResult,
	CompactRowsResult as CompactRowsResult,
	Conv1dBwdWeightResult as Conv1dBwdWeightResult,
	Conv2dBwdWeightResult as Conv2dBwdWeightResult,
	ConvTranspose2dBwdWeightResult as ConvTranspose2dBwdWeightResult,
	EmpyrealmAdtBwdResult as EmpyrealmAdtBwdResult,
	EmpyrealmDtAdtResult as EmpyrealmDtAdtResult,
	FlashAttentionBwdResult as FlashAttentionBwdResult,
	GroupedGemmMBwdResult as GroupedGemmMBwdResult,
	GroupedLinearMBwdResult as GroupedLinearMBwdResult,
	GruCellPointwiseBwdResult as GruCellPointwiseBwdResult,
	GruScanBwdResult as GruScanBwdResult,
	GruScanResult as GruScanResult,
	LinearWeightBiasBwdResult as LinearWeightBiasBwdResult,
	Mamba3MimoBwdResult as Mamba3MimoBwdResult,
	Mamba3PreprocessBwdResult as Mamba3PreprocessBwdResult,
	Mamba3PreprocessConfig as Mamba3PreprocessConfig,
	Mamba3PreprocessResult as Mamba3PreprocessResult,
	MaxPool2dResult as MaxPool2dResult,
	MoeCombineBwdResult as MoeCombineBwdResult,
	MoeExpertPlan as MoeExpertPlan,
	QuantMatrix as QuantMatrix,
	Quantization as Quantization,
	ResidualRmsNormResult as ResidualRmsNormResult,
	RmsNormGatedBwdResult as RmsNormGatedBwdResult,
	RnnCellPointwiseBwdResult as RnnCellPointwiseBwdResult,
	RnnScanBwdResult as RnnScanBwdResult,
	RnnScanResult as RnnScanResult,
	SsmBwdResult as SsmBwdResult,
	SsmConfig as SsmConfig,
	TopKResult as TopKResult,
	VqAssignResult as VqAssignResult
)


def empty(shape: Sequence[int], dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def zeros(shape: Sequence[int], dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def zeros(d0: int, d1: int, dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def ones(shape: Sequence[int], dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def ones(d0: int, d1: int, dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def full(shape: Sequence[int], value: float, dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def full(d0: int, d1: int, value: float, dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def rand(shape: Sequence[int], dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def rand(d0: int, d1: int, dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def randN(shape: Sequence[int], dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def randN(d0: int, d1: int, dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def randXavier(shape: Sequence[int], dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def randXavier(d0: int, d1: int, dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def randKaimingUniform(shape: Sequence[int], dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def randKaimingUniform(d0: int, d1: int, dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def randGlorotUniform(shape: Sequence[int], dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

@overload
def randGlorotUniform(d0: int, d1: int, dtype: oa.ScalarType = oa.ScalarType.Float32) -> oa.Matrix: ...

def weightDtype() -> oa.ScalarType:
	"""Get the current weight dtype (Float32, BFloat16, or Float16)"""

def setRngSeed(seed: int) -> None: ...

def abs(a: oa.Matrix) -> oa.Matrix:
	"""Element-wise absolute value: out = |A|."""

def add(a: oa.Matrix, b: oa.Matrix) -> oa.Matrix:
	"""Element-wise matrix addition with broadcasting."""

def addScalar(a: oa.Matrix, scalar: float) -> oa.Matrix:
	"""Scalar addition: out = A + Scalar."""

def cast(src: oa.Matrix, dtype: oa.ScalarType) -> oa.Matrix:
	"""Convert A to Dtype; returns an alias when the dtype is unchanged."""

def categoricalAccuracyCount(logits: oa.Matrix, labels: oa.Matrix) -> oa.Matrix:
	"""
	Count correct last-axis categorical predictions into one device UInt32 scalar.
	"""

@overload
def causalMask(scores: oa.Matrix) -> oa.Matrix:
	"""Apply an additive causal mask to Scores."""

@overload
def causalMask(seqLen: int) -> oa.Matrix: ...

def clampMax(a: oa.Matrix, max: float) -> oa.Matrix:
	"""Element-wise clamp max: out = min(A, Max)."""

def clampMin(a: oa.Matrix, min: float) -> oa.Matrix:
	"""Element-wise clamp min: out = max(A, Min)."""

def compactRows(self: oa.Matrix, mask: oa.Matrix) -> oa._oa.FnMatrix.CompactRowsResult:
	"""
	Compact selected rows while retaining device-authored row-map and count metadata.
	"""

def concat(inputs: Sequence[oa.Matrix], dim: int = ...) -> oa.Matrix:
	"""Concatenate one or more matrices along Dim."""

def copy(a: oa.Matrix) -> oa.Matrix:
	"""Create an independent device copy of a matrix."""

def cos(a: oa.Matrix) -> oa.Matrix:
	"""Element-wise cosine: out = cos(A)."""

def detach(self: oa.Matrix) -> oa.Matrix:
	"""Return a storage-sharing stop-gradient view of A."""

def div(a: oa.Matrix, b: oa.Matrix) -> oa.Matrix:
	"""Element-wise division: out = A / B."""

def divScalar(a: oa.Matrix, scalar: float) -> oa.Matrix:
	"""Scalar division: out = A / Scalar."""

def dropout(a: oa.Matrix, p: float, seed: int = ...) -> oa.Matrix:
	"""Apply inverted dropout to A with an optional explicit Philox seed."""

def equal(a: oa.Matrix, value: float) -> oa.Matrix:
	"""Return a Float32 mask where A equals Value."""

def exp(a: oa.Matrix) -> oa.Matrix:
	"""Element-wise exponential: out = exp(A)."""

def fill(shape: oa.MatrixShape, value: float) -> oa.Matrix:
	"""Create a Float32 matrix of Shape filled with Value."""

def gather(a: oa.Matrix, indices: oa.Matrix) -> oa.Matrix:
	"""Gather rows from a rank-two table using UInt8 or UInt32 indices."""

def gatherLastDim(self: oa.Matrix, indices: oa.Matrix) -> oa.Matrix:
	"""Gather Int32-selected values from the last dimension of Self."""

def greaterEqual(a: oa.Matrix, value: float) -> oa.Matrix:
	"""Return a Float32 mask where A is greater than or equal to Value."""

def log(a: oa.Matrix) -> oa.Matrix:
	"""Element-wise natural log: out = log(A)."""

def logSoftmax(a: oa.Matrix, dim: int = ...) -> oa.Matrix:
	"""
	Stable log-softmax over the selected dimension; -1 selects the last dimension.
	"""

def maskedCategoricalAccuracyCount(logits: oa.Matrix, labels: oa.Matrix, mask: oa.Matrix) -> oa.Matrix:
	"""
	Count correct masked categorical predictions into one device UInt32 scalar.
	"""

@overload
def matMulNt(a: oa.Matrix, b: oa.Matrix, precision: oa.MatMulPrecision = oa.MatMulPrecision.Auto) -> oa.Matrix:
	"""
	oa::Matrix multiplication C = A @ B^T using the OA weight-layout convention.
	"""

@overload
def matMulNt(input: oa.Matrix, weight: oa._oa.FnMatrix.QuantMatrix) -> oa.Matrix: ...

def max(a: oa.Matrix, dim: int = ...) -> oa.Matrix:
	"""
	Reduce all matrix values by maximum; dim is currently a compatibility parameter.
	"""

def mean(a: oa.Matrix, dim: int = ...) -> oa.Matrix:
	"""
	Reduce by arithmetic mean, preserving a selected dimension or reducing all values when dim is -1.
	"""

def moeExpertPlan(expertIndices: oa.Matrix, numExperts: int) -> oa._oa.FnMatrix.MoeExpertPlan:
	"""Build a stable dropless expert-major route plan."""

def mul(a: oa.Matrix, b: oa.Matrix) -> oa.Matrix:
	"""Element-wise multiplication: out = A * B."""

def neg(a: oa.Matrix) -> oa.Matrix:
	"""Element-wise negation: out = -A."""

def philoxNormal(a: oa.Matrix, mean: float = ..., stddev: float = ..., seed: int = ...) -> oa.Matrix:
	"""Generate a Philox normal sample with the input shape and dtype."""

def philoxUniform(a: oa.Matrix, low: float = ..., high: float = ..., seed: int = ...) -> oa.Matrix:
	"""Generate a Philox uniform sample with the input shape and dtype."""

def pow(a: oa.Matrix, exponent: float) -> oa.Matrix:
	"""Element-wise power: out = A^Exponent."""

def reciprocal(a: oa.Matrix) -> oa.Matrix:
	"""Element-wise reciprocal: out = 1 / A."""

def repeatInterleave(a: oa.Matrix, repeats: int, dim: int) -> oa.Matrix:
	"""Repeat every value in A along Dim."""

def sampleLogits(logits: oa.Matrix, temperature: float = ..., topK: int = ..., topP: float = ..., seed: int = ...) -> oa.Matrix:
	"""Sample one class per final-axis row of Logits."""

def scale(a: oa.Matrix, scalar: float) -> oa.Matrix:
	"""Scalar multiplication: out = A * Scalar."""

def scatterRows(self: oa.Matrix, source: oa.Matrix, rowMap: oa.Matrix, count: oa.Matrix) -> oa.Matrix:
	"""
	Scatter compacted Source rows over Self using a device row map and count.
	"""

def sin(a: oa.Matrix) -> oa.Matrix:
	"""Element-wise sine: out = sin(A)."""

def slice(self: oa.Matrix, dim: int, start: int, end: int) -> oa.Matrix:
	"""Materialize a contiguous half-open slice [Start, End) along Dim."""

def softmax(a: oa.Matrix, dim: int = ...) -> oa.Matrix:
	"""
	Stable softmax over the selected dimension; -1 selects the last dimension.
	"""

def softmaxBwd(forwardOutput: oa.Matrix, gradOutput: oa.Matrix, dim: int = ...) -> oa.Matrix:
	"""Apply the axis-aware softmax adjoint to an explicit output gradient."""

def split(self: oa.Matrix, sizes: Sequence[int], dim: int = ...) -> list[oa.Matrix]:
	"""
	Split Self into materialized outputs with the requested Sizes along Dim.
	"""

def sqrt(a: oa.Matrix) -> oa.Matrix:
	"""Element-wise square root: out = sqrt(A)."""

def sub(a: oa.Matrix, b: oa.Matrix) -> oa.Matrix:
	"""Element-wise subtraction: out = A - B."""

def subScalar(a: oa.Matrix, scalar: float) -> oa.Matrix:
	"""Scalar subtraction: out = A - Scalar."""

def sum(a: oa.Matrix, dim: int = ...) -> oa.Matrix:
	"""
	Reduce by summation, preserving a selected dimension or reducing all values when dim is -1.
	"""

def topK(a: oa.Matrix, k: int, dim: int = ...) -> oa._oa.FnMatrix.TopKResult:
	"""
	Return the largest K values and their Int32 indices along the last dimension.
	"""

def topKMask(indices: oa.Matrix, numExperts: int) -> oa.Matrix:
	"""Expand TopK indices into a dense expert-membership mask."""

def transpose(a: oa.Matrix, dim0: int = ..., dim1: int = ...) -> oa.Matrix:
	"""Materialize a swap of the last two axes."""

def argmax(a: oa.Matrix, dim: int = ...) -> int: ...

def scalar(mat: oa.Matrix) -> float: ...

def copyToHost(mat: oa.Matrix) -> list:
	"""Copy a device matrix to a Python list without reinterpreting its dtype"""

def copyToHost2D(mat: oa.Matrix, rows: int, cols: int) -> list[list[float]]:
	"""Copy device matrix to host as a 2D list of float32 values"""

@overload
def reshape(a: oa.Matrix, shape: Sequence[int]) -> oa.Matrix: ...

@overload
def reshape(a: oa.Matrix, d0: int, d1: int) -> oa.Matrix: ...

@overload
def reshape(a: oa.Matrix, d0: int) -> oa.Matrix: ...

@overload
def fromBytes(data: Sequence[int], shape: Sequence[int], dtype: oa.ScalarType) -> oa.Matrix:
	"""Create matrix from host byte data"""

@overload
def fromBytes(data: Sequence[int], d0: int, d1: int, dtype: oa.ScalarType) -> oa.Matrix:
	"""Create 2D matrix from host byte data"""

@overload
def fromBytes(data: Sequence[int], d0: int, dtype: oa.ScalarType) -> oa.Matrix:
	"""Create 1D matrix from host byte data"""

def fromInt32(data: Sequence[int], shape: Sequence[int], dtype: oa.ScalarType = oa.ScalarType.Int32) -> oa.Matrix:
	"""Create matrix from host int32 data"""

@overload
def fromFloats(data: Sequence[float], shape: Sequence[int]) -> oa.Matrix:
	"""Create a Float32 matrix from host float data"""

@overload
def fromFloats(data: Sequence[float], d0: int, d1: int) -> oa.Matrix:
	"""Create a 2D Float32 matrix from host float data"""

@overload
def fromFloats(data: Sequence[float], d0: int) -> oa.Matrix:
	"""Create a 1D Float32 matrix from host float data"""

def linearWeightBiasBwd(input: oa.Matrix, gradOutput: oa.Matrix) -> oa._oa.FnMatrix.LinearWeightBiasBwdResult: ...

def linearDataBwd(gradOutput: oa.Matrix, weight: oa.Matrix) -> oa.Matrix: ...

def gatherBwd(indices: oa.Matrix, gradOutput: oa.Matrix, vocabSize: int, embedDim: int) -> oa.Matrix: ...

def quantize(input: oa.Matrix, quantization: oa._oa.FnMatrix.Quantization) -> oa._oa.FnMatrix.QuantMatrix: ...

def dequantize(input: oa._oa.FnMatrix.QuantMatrix) -> oa.Matrix: ...

def avgPool2d(x: oa.Matrix, kernelSize: int, stride: int, padding: int) -> oa.Matrix:
	"""Apply 2D average pooling to an NCHW matrix."""

def biasAdd(a: oa.Matrix, b: oa.Matrix) -> oa.Matrix:
	"""Add bias vector: out = A + Bias (broadcasted)."""

def bmm(a: oa.Matrix, b: oa.Matrix) -> oa.Matrix:
	"""Multiply corresponding matrices in two rank-three batches."""

def bmmNt(a: oa.Matrix, b: oa.Matrix) -> oa.Matrix:
	"""
	Multiply corresponding rank-three batches with the right operand stored as [N,P,K].
	"""

def channelNorm(x: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix, batch: int, channels: int, seqLen: int, eps: float) -> oa.Matrix:
	"""Normalize channels independently for every BCT position."""

def channelNormBwd(x: oa.Matrix, weight: oa.Matrix, gradOutput: oa.Matrix, batch: int, channels: int, seqLen: int, eps: float) -> oa._oa.FnMatrix.ChannelNormBwdResult:
	"""Compute explicit channel-normalization gradients."""

def channelNormRelu(x: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix, batch: int, channels: int, seqLen: int, eps: float) -> oa.Matrix:
	"""Apply fused channel normalization and ReLU."""

def channelNormReluBwd(x: oa.Matrix, weight: oa.Matrix, fwdOut: oa.Matrix, gradOutput: oa.Matrix, batch: int, channels: int, seqLen: int, eps: float) -> oa._oa.FnMatrix.ChannelNormBwdResult:
	"""Compute explicit fused channel-normalization and ReLU gradients."""

def col2Im1d(dCols: oa.Matrix, n: int, c: int, l: int, k: int, stride: int, padding: int, dilation: int, outL: int) -> oa.Matrix:
	"""Fold a 1D convolution column gradient back into NCL storage."""

def conv1dBwdData(dOut: oa.Matrix, weight: oa.Matrix, stride: int, padding: int, dilation: int, inputShape: oa.MatrixShape) -> oa.Matrix:
	"""Compute the explicit 1D convolution input gradient."""

def conv1dBwdWeight(input: oa.Matrix, dOut: oa.Matrix, weight: oa.Matrix, stride: int, padding: int, dilation: int) -> oa._oa.FnMatrix.Conv1dBwdWeightResult:
	"""Compute explicit 1D convolution weight and bias gradients."""

def conv1dGemm(x: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix, stride: int = ..., padding: int = ..., dilation: int = ...) -> oa.Matrix:
	"""Apply 1D convolution through the canonical im2col and GEMM lowering."""

def conv1dReluGemm(x: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix, stride: int = ..., padding: int = ..., dilation: int = ...) -> oa.Matrix:
	"""
	Apply 1D convolution followed by ReLU through the canonical GEMM lowering.
	"""

def conv2d(x: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix, stride: int, padding: int, groups: int = ...) -> oa.Matrix:
	"""Apply grouped 2D convolution to an NCHW input."""

def conv2dBwdData(dOut: oa.Matrix, weight: oa.Matrix, stride: int, padding: int, inputShape: oa.MatrixShape, groups: int = ...) -> oa.Matrix:
	"""Compute the explicit grouped 2D convolution input gradient."""

def conv2dBwdWeight(input: oa.Matrix, dOut: oa.Matrix, weight: oa.Matrix, stride: int, padding: int, groups: int = ...) -> oa._oa.FnMatrix.Conv2dBwdWeightResult:
	"""Compute explicit grouped 2D convolution weight and bias gradients."""

def convTranspose2d(x: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix, stride: int, padding: int) -> oa.Matrix:
	"""Apply 2D transposed convolution to an NCHW input."""

def convTranspose2dBwdData(dOut: oa.Matrix, weight: oa.Matrix, stride: int, padding: int, inputShape: oa.MatrixShape) -> oa.Matrix:
	"""Compute the explicit 2D transposed-convolution input gradient."""

def convTranspose2dBwdWeight(input: oa.Matrix, dOut: oa.Matrix, weight: oa.Matrix, stride: int, padding: int) -> oa._oa.FnMatrix.ConvTranspose2dBwdWeightResult:
	"""Compute explicit 2D transposed-convolution weight and bias gradients."""

def elu(a: oa.Matrix, alpha: float) -> oa.Matrix:
	"""Exponential linear unit activation."""

def empyrealmAdt(ddA: oa.Matrix, dt: oa.Matrix, aFloor: float) -> oa.Matrix:
	"""Compute the fused Empyrealm heavy-tail A-times-dt term."""

def empyrealmAdtBwd(dOut: oa.Matrix, ddA: oa.Matrix, dt: oa.Matrix, aFloor: float) -> oa._oa.FnMatrix.EmpyrealmAdtBwdResult:
	"""Compute explicit gradients of the fused Empyrealm A-times-dt term."""

def empyrealmDt(x: oa.Matrix, dtMin: float, dtMax: float) -> oa.Matrix:
	"""Apply the fused Empyrealm softplus and dt clamp."""

def empyrealmDtAdt(dtRaw: oa.Matrix, ddA: oa.Matrix, dtMin: float, dtMax: float, aFloor: float) -> oa._oa.FnMatrix.EmpyrealmDtAdtResult:
	"""Compute fused Empyrealm dt and A-times-dt values."""

def empyrealmDtBwd(dOut: oa.Matrix, x: oa.Matrix, dtMin: float, dtMax: float) -> oa.Matrix:
	"""Compute the explicit gradient of the fused Empyrealm dt term."""

def empyrealmPreprocess(projected: oa.Matrix, dtBias: oa.Matrix, config: oa._oa.FnMatrix.Mamba3PreprocessConfig) -> oa._oa.FnMatrix.Mamba3PreprocessResult:
	"""Run the fused Empyrealm projection preprocess."""

def empyrealmPreprocessBwd(projected: oa.Matrix, dtBias: oa.Matrix, dZ: oa.Matrix, dX: oa.Matrix, dBh: oa.Matrix, dCh: oa.Matrix, dDT: oa.Matrix, dADT: oa.Matrix, dTrap: oa.Matrix, dAngle: oa.Matrix, config: oa._oa.FnMatrix.Mamba3PreprocessConfig) -> oa._oa.FnMatrix.Mamba3PreprocessBwdResult:
	"""Compute the fused Empyrealm preprocess gradients."""

def empyrealmSiso(c: oa.Matrix, b: oa.Matrix, x: oa.Matrix, z: oa.Matrix, adt: oa.Matrix, dt: oa.Matrix, trap: oa.Matrix, angle: oa.Matrix, cBias: oa.Matrix, bBias: oa.Matrix, d: oa.Matrix, config: oa._oa.FnMatrix.SsmConfig) -> oa.Matrix:
	"""Run the full-sequence Empyrealm selective state-space scan."""

def empyrealmSisoBwd(dOut: oa.Matrix, c: oa.Matrix, b: oa.Matrix, x: oa.Matrix, z: oa.Matrix, adt: oa.Matrix, dt: oa.Matrix, trap: oa.Matrix, angle: oa.Matrix, cBias: oa.Matrix, bBias: oa.Matrix, d: oa.Matrix, config: oa._oa.FnMatrix.SsmConfig) -> oa._oa.FnMatrix.SsmBwdResult:
	"""Compute all explicit Empyrealm selective-scan gradients."""

def empyrealmSisoStep(c: oa.Matrix, b: oa.Matrix, x: oa.Matrix, z: oa.Matrix, adt: oa.Matrix, dt: oa.Matrix, trap: oa.Matrix, angle: oa.Matrix, cBias: oa.Matrix, bBias: oa.Matrix, d: oa.Matrix, ssmState: oa.Matrix, angleState: oa.Matrix, kState: oa.Matrix, vState: oa.Matrix, config: oa._oa.FnMatrix.SsmConfig) -> oa.Matrix:
	"""
	Run one autoregressive Empyrealm step and update the supplied recurrent states.
	"""

def flashAttentionCausal(q: oa.Matrix, k: oa.Matrix, v: oa.Matrix, scale: float) -> oa.Matrix:
	"""
	Apply causal scaled dot-product attention to contiguous [batchHeads, sequence, headDim] matrices.
	"""

def flashAttentionCausalBwd(q: oa.Matrix, k: oa.Matrix, v: oa.Matrix, output: oa.Matrix, logSumExp: oa.Matrix, gradOutput: oa.Matrix, scale: float) -> oa._oa.FnMatrix.FlashAttentionBwdResult:
	"""Compute the explicit Q, K, and V adjoints for causal flash attention."""

def geglu(a: oa.Matrix, intermediateSize: int) -> oa.Matrix:
	"""Apply GEGLU to two halves of the final matrix dimension."""

def gelu(a: oa.Matrix) -> oa.Matrix:
	"""Gaussian error linear unit activation."""

def geluBwd(input: oa.Matrix, gradOutput: oa.Matrix) -> oa.Matrix:
	"""Explicit GELU adjoint using the saved forward input."""

def groupedGemmM(x: oa.Matrix, weight: oa.Matrix, offsets: oa.Matrix) -> oa.Matrix:
	"""Apply an expert-major variable-row grouped matrix multiplication."""

def groupedGemmMBwd(dOut: oa.Matrix, x: oa.Matrix, weight: oa.Matrix, offsets: oa.Matrix) -> oa._oa.FnMatrix.GroupedGemmMBwdResult:
	"""Compute grouped GEMM input and weight adjoints."""

def groupedLinearM(x: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix, offsets: oa.Matrix) -> oa.Matrix:
	"""Apply an expert-major variable-row grouped linear projection."""

def groupedLinearMBiasBwd(dOut: oa.Matrix, offsets: oa.Matrix, numExperts: int) -> oa.Matrix:
	"""Reduce grouped output gradients into one bias-gradient row per expert."""

def groupedLinearMBwd(dOut: oa.Matrix, x: oa.Matrix, weight: oa.Matrix, offsets: oa.Matrix) -> oa._oa.FnMatrix.GroupedLinearMBwdResult:
	"""Compute grouped linear input, weight, and bias adjoints."""

def gruCellLinear(gatesI: oa.Matrix, hidden: oa.Matrix, weightHh: oa.Matrix, hiddenSize: int, timeOffset: int, batchStride: int, biasHh: oa.Matrix = ...) -> oa.Matrix:
	"""Apply one fused GRU recurrent projection and update."""

def gruCellPointwise(gatesI: oa.Matrix, gatesH: oa.Matrix, hidden: oa.Matrix, hiddenSize: int, timeOffset: int = ..., batchStride: int = ...) -> oa.Matrix:
	"""Apply one fused GRU pointwise update."""

def gruCellPointwiseBwd(gatesI: oa.Matrix, gatesH: oa.Matrix, hidden: oa.Matrix, gradOutput: oa.Matrix, hiddenSize: int, timeOffset: int = ..., batchStride: int = ...) -> oa._oa.FnMatrix.GruCellPointwiseBwdResult:
	"""Compute explicit GRU pointwise gradients."""

def gruScan(gatesI: oa.Matrix, weightHh: oa.Matrix, hiddenSize: int, seqLen: int, batch: int, biasHh: oa.Matrix = ...) -> oa._oa.FnMatrix.GruScanResult:
	"""
	Run a whole GRU sequence and return its output and saved previous hidden states.
	"""

def gruScanBwd(dOut: oa.Matrix, gatesI: oa.Matrix, hprev: oa.Matrix, weightHh: oa.Matrix, hiddenSize: int, seqLen: int, batch: int, biasHh: oa.Matrix = ...) -> oa._oa.FnMatrix.GruScanBwdResult:
	"""Compute explicit GRU scan projection gradients."""

def heavyTailActivation(self: oa.Matrix) -> oa.Matrix:
	"""Apply the positive state-space heavy-tail activation."""

def im2Col1d(x: oa.Matrix, k: int, stride: int, padding: int, dilation: int = ...) -> oa.Matrix:
	"""Unfold an NCL tensor into the canonical 1D convolution column matrix."""

def layerNorm(self: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix, eps: float) -> oa.Matrix:
	"""
	Affine layer normalization over the last dimension: out = ((A - mean(A)) / sqrt(var(A) + Eps)) * Weight + Bias.
	"""

def leakyRelu(a: oa.Matrix, alpha: float) -> oa.Matrix:
	"""Leaky rectified linear unit activation."""

def linear(x: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix = ...) -> oa.Matrix:
	"""
	Apply a linear projection using the OA weight-layout convention and an optional bias.
	"""

def linearGelu(x: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix) -> oa.Matrix:
	"""Apply a fused linear projection and GELU activation."""

def linearRelu(x: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix) -> oa.Matrix:
	"""Apply a fused linear projection and ReLU activation."""

def linearSilu(x: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix) -> oa.Matrix:
	"""Apply a fused linear projection and SiLU activation."""

def mamba3Mimo(c: oa.Matrix, b: oa.Matrix, x: oa.Matrix, z: oa.Matrix, adt: oa.Matrix, dt: oa.Matrix, trap: oa.Matrix, angle: oa.Matrix, cBias: oa.Matrix, bBias: oa.Matrix, d: oa.Matrix, mimoX: oa.Matrix, mimoZ: oa.Matrix, mimoO: oa.Matrix, normWeight: oa.Matrix, config: oa._oa.FnMatrix.SsmConfig) -> oa.Matrix:
	"""Run the shared-state Mamba-3 MIMO selective scan."""

def mamba3MimoBwd(dOut: oa.Matrix, c: oa.Matrix, b: oa.Matrix, x: oa.Matrix, z: oa.Matrix, adt: oa.Matrix, dt: oa.Matrix, trap: oa.Matrix, angle: oa.Matrix, cBias: oa.Matrix, bBias: oa.Matrix, d: oa.Matrix, mimoX: oa.Matrix, mimoZ: oa.Matrix, mimoO: oa.Matrix, normWeight: oa.Matrix, config: oa._oa.FnMatrix.SsmConfig) -> oa._oa.FnMatrix.Mamba3MimoBwdResult:
	"""Compute all explicit Mamba-3 MIMO gradients."""

def mamba3MimoStep(c: oa.Matrix, b: oa.Matrix, x: oa.Matrix, z: oa.Matrix, adt: oa.Matrix, dt: oa.Matrix, trap: oa.Matrix, angle: oa.Matrix, cBias: oa.Matrix, bBias: oa.Matrix, d: oa.Matrix, mimoX: oa.Matrix, mimoZ: oa.Matrix, mimoO: oa.Matrix, normWeight: oa.Matrix, ssmState: oa.Matrix, angleState: oa.Matrix, kState: oa.Matrix, vState: oa.Matrix, config: oa._oa.FnMatrix.SsmConfig) -> oa.Matrix:
	"""Run one shared-state Mamba-3 MIMO decode step."""

def mamba3Preprocess(projected: oa.Matrix, dtBias: oa.Matrix, config: oa._oa.FnMatrix.Mamba3PreprocessConfig) -> oa._oa.FnMatrix.Mamba3PreprocessResult:
	"""Run the fused Mamba-3 projection preprocess."""

def mamba3PreprocessBwd(projected: oa.Matrix, dtBias: oa.Matrix, dZ: oa.Matrix, dX: oa.Matrix, dBh: oa.Matrix, dCh: oa.Matrix, dDT: oa.Matrix, dADT: oa.Matrix, dTrap: oa.Matrix, dAngle: oa.Matrix, config: oa._oa.FnMatrix.Mamba3PreprocessConfig) -> oa._oa.FnMatrix.Mamba3PreprocessBwdResult:
	"""Compute the fused Mamba-3 preprocess gradients."""

def mamba3Siso(c: oa.Matrix, b: oa.Matrix, x: oa.Matrix, z: oa.Matrix, adt: oa.Matrix, dt: oa.Matrix, trap: oa.Matrix, angle: oa.Matrix, cBias: oa.Matrix, bBias: oa.Matrix, d: oa.Matrix, config: oa._oa.FnMatrix.SsmConfig) -> oa.Matrix:
	"""Run the full-sequence Mamba-3 selective state-space scan."""

def mamba3SisoBwd(dOut: oa.Matrix, c: oa.Matrix, b: oa.Matrix, x: oa.Matrix, z: oa.Matrix, adt: oa.Matrix, dt: oa.Matrix, trap: oa.Matrix, angle: oa.Matrix, cBias: oa.Matrix, bBias: oa.Matrix, d: oa.Matrix, config: oa._oa.FnMatrix.SsmConfig) -> oa._oa.FnMatrix.SsmBwdResult:
	"""Compute all explicit Mamba-3 selective-scan gradients."""

def mamba3SisoStep(c: oa.Matrix, b: oa.Matrix, x: oa.Matrix, z: oa.Matrix, adt: oa.Matrix, dt: oa.Matrix, trap: oa.Matrix, angle: oa.Matrix, cBias: oa.Matrix, bBias: oa.Matrix, d: oa.Matrix, ssmState: oa.Matrix, angleState: oa.Matrix, kState: oa.Matrix, vState: oa.Matrix, config: oa._oa.FnMatrix.SsmConfig) -> oa.Matrix:
	"""
	Run one autoregressive Mamba-3 step and update the supplied recurrent states.
	"""

def maxPool2d(x: oa.Matrix, kernelSize: int, stride: int, padding: int) -> oa._oa.FnMatrix.MaxPool2dResult:
	"""2D max pooling: out = max(A[window])."""

def mergeHeads(x: oa.Matrix, batch: int, seqLen: int, numHeads: int) -> oa.Matrix:
	"""
	Inverse of SplitHeads: [B*H,S,D/H] -> [B*S,D]; H=1 is a metadata-only view.
	"""

def mish(a: oa.Matrix) -> oa.Matrix:
	"""Mish activation."""

def moeCombine(packed: oa.Matrix, routeGate: oa.Matrix, inverse: oa.Matrix, packedSlot: oa.Matrix) -> oa.Matrix:
	"""Combine expert-major packed rows into token-major output."""

def moeCombineBwd(dOut: oa.Matrix, packed: oa.Matrix, routeGate: oa.Matrix, inverse: oa.Matrix, packedSlot: oa.Matrix) -> oa._oa.FnMatrix.MoeCombineBwdResult:
	"""Compute packed-value and route-gate adjoints for MoE combine."""

def moeGather(self: oa.Matrix, indices: oa.Matrix, inverse: oa.Matrix) -> oa.Matrix:
	"""Pack token rows into expert-major order using a stable route plan."""

def moeGatherBwd(source: oa.Matrix, inverse: oa.Matrix, outRows: int) -> oa.Matrix:
	"""Reduce packed row gradients back into token-major rows."""

def moeRouteWeights(probs: oa.Matrix, expertIndices: oa.Matrix) -> oa.Matrix:
	"""Normalize the probabilities of the selected experts for every token."""

def moeRouteWeightsBwd(dOut: oa.Matrix, probs: oa.Matrix, expertIndices: oa.Matrix, routeWeights: oa.Matrix) -> oa.Matrix:
	"""
	Compute the explicit dense probability adjoint for selected MoE routes.
	"""

def relu(a: oa.Matrix) -> oa.Matrix:
	"""Rectified linear unit activation."""

def reluBwd(forwardOutput: oa.Matrix, gradOutput: oa.Matrix) -> oa.Matrix:
	"""Explicit ReLU adjoint."""

def residualRmsNorm(a: oa.Matrix, b: oa.Matrix, weight: oa.Matrix, eps: float) -> oa._oa.FnMatrix.ResidualRmsNormResult:
	"""Fused residual + RMS norm: out = (A + B) / sqrt(mean((A+B)^2) + Eps)."""

def rmsNorm(self: oa.Matrix, weight: oa.Matrix, eps: float) -> oa.Matrix:
	"""
	Weighted root mean square normalization over the last dimension: out = (A / sqrt(mean(A^2) + Eps)) * Weight.
	"""

def rmsNormGated(self: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix, z: oa.Matrix, eps: float, normBeforeGate: bool) -> oa.Matrix:
	"""Apply fused gated RMS normalization."""

def rmsNormGatedBwd(x: oa.Matrix, weight: oa.Matrix, bias: oa.Matrix, z: oa.Matrix, gradOutput: oa.Matrix, eps: float) -> oa._oa.FnMatrix.RmsNormGatedBwdResult:
	"""Compute explicit gated RMS normalization gradients."""

def rnnCellLinear(gatesI: oa.Matrix, hidden: oa.Matrix, weightHh: oa.Matrix, timeOffset: int = ..., batchStride: int = ..., biasHh: oa.Matrix = ...) -> oa.Matrix:
	"""Apply one fused vanilla-RNN recurrent projection and update."""

def rnnCellPointwise(gatesI: oa.Matrix, gatesH: oa.Matrix) -> oa.Matrix:
	"""Apply the fused tanh update for one contiguous vanilla-RNN timestep."""

def rnnCellPointwiseBwd(gatesI: oa.Matrix, gatesH: oa.Matrix, gradOutput: oa.Matrix, hiddenSize: int, timeOffset: int = ..., batchStride: int = ...) -> oa._oa.FnMatrix.RnnCellPointwiseBwdResult:
	"""Compute explicit vanilla-RNN pointwise gradients."""

def rnnScan(gatesI: oa.Matrix, weightHh: oa.Matrix, hiddenSize: int, seqLen: int, batch: int, biasHh: oa.Matrix = ...) -> oa._oa.FnMatrix.RnnScanResult:
	"""
	Run a whole vanilla-RNN sequence and return its output and saved previous hidden states.
	"""

def rnnScanBwd(dOut: oa.Matrix, gatesI: oa.Matrix, hprev: oa.Matrix, weightHh: oa.Matrix, hiddenSize: int, seqLen: int, batch: int, biasHh: oa.Matrix = ...) -> oa._oa.FnMatrix.RnnScanBwdResult:
	"""Compute explicit vanilla-RNN scan projection gradients."""

def scatterAddRows(source: oa.Matrix, indices: oa.Matrix, outRows: int) -> oa.Matrix:
	"""Accumulate indexed source rows into a zero-initialized output."""

def sigmoid(a: oa.Matrix) -> oa.Matrix:
	"""Logistic sigmoid activation."""

def silu(a: oa.Matrix) -> oa.Matrix:
	"""SiLU (Swish) activation."""

def siluBwd(input: oa.Matrix, gradOutput: oa.Matrix) -> oa.Matrix:
	"""Explicit SiLU adjoint using the saved forward input."""

def siluMul(a: oa.Matrix, intermediateSize: int) -> oa.Matrix:
	"""
	Apply SiLU to the first half of the final dimension and multiply it by the second half.
	"""

def softmaxScaledMasked(scores: oa.Matrix, mask: oa.Matrix, scale: float) -> oa.Matrix:
	"""Fused scale, additive mask, and final-axis softmax."""

def softplus(a: oa.Matrix) -> oa.Matrix:
	"""Numerically stable softplus activation."""

def splitHeads(x: oa.Matrix, batch: int, seqLen: int, numHeads: int) -> oa.Matrix:
	"""
	Transform contiguous [B*S,D] storage to [B*H,S,D/H]; H=1 is a metadata-only view.
	"""

def swiglu(a: oa.Matrix, b: oa.Matrix) -> oa.Matrix:
	"""SwiGLU activation from separate gate and up matrices."""

def tanh(a: oa.Matrix) -> oa.Matrix:
	"""Hyperbolic tangent activation."""

def tanhBwd(forwardOutput: oa.Matrix, gradOutput: oa.Matrix) -> oa.Matrix:
	"""Explicit tanh adjoint."""

def vqAssign(ze: oa.Matrix, codebook: oa.Matrix) -> oa._oa.FnMatrix.VqAssignResult:
	"""Assign latent rows to their nearest vector-quantization codes."""

def vqEmaUpdate(ze: oa.Matrix, idx: oa.Matrix, inOutEmbedSum: oa.Matrix, inOutClusterSize: oa.Matrix, codebook: oa.Matrix, decay: float, eps: float, deadThreshold: float, seed: int, normalize: bool) -> None:
	"""
	Update vector-quantization EMA statistics and codebook storage in place.
	"""
