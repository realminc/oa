# AUTO-GENERATED by tools/python/oapystubgen.py — DO NOT EDIT.
# Sources of truth: the live nanobind signatures and oa/_surface.py.

"""Native Python view of C++ namespace oa::FnAudio"""

from collections.abc import Sequence

import oa


def decodeFile(path: object) -> oa.Audio:
	"""
	Decode a WAV/FLAC/MP3 file to a planar [Channels, Samples] F32 GPU buffer.
	"""

def decodeMemory(data: bytes) -> oa.Audio:
	"""Decode audio from an in-memory byte buffer (bytes; WAV/FLAC/MP3)."""

def encodeWavF32(audio: oa.Audio) -> bytes:
	"""Encode an oa::Audio value to WAV-F32 bytes (synchronous)."""

def saveWavF32(path: object, audio: oa.Audio) -> None:
	"""
	Encode + write a lossless WAV-F32 file (synchronous file/codec boundary).
	"""

def amplitudeToDb(audio: oa.Audio, floorDb: float = ...) -> oa.Matrix:
	"""Convert waveform amplitude to a finite dB matrix with a silence floor."""

def biquad(audio: oa.Audio, coefficients: oa.BiquadCoefficients = ...) -> oa.Audio:
	"""
	Apply one stable a0-normalized biquad independently per channel with zero initial state.
	"""

def clip(audio: oa.Audio, min: float = ..., max: float = ...) -> oa.Audio:
	"""
	Clamp waveform samples to a finite range while preserving audio metadata.
	"""

def fade(audio: oa.Audio, fadeInSamples: int, fadeOutSamples: int) -> oa.Audio:
	"""Apply linear fade-in and fade-out envelopes measured in samples."""

def gain(audio: oa.Audio, gainDb: float) -> oa.Audio:
	"""Apply scalar gain in dB while preserving audio metadata."""

def melSpectrogram(audio: oa.Audio, cfg: oa.MelConfig = ...) -> oa.Matrix:
	"""Mel feature matrix in [Channels, NumMels, Frames] layout."""

def mfcc(audio: oa.Audio, cfg: oa.MfccConfig = ...) -> oa.Matrix:
	"""Orthonormal DCT-II coefficients of the log-mel feature matrix."""

def mix(a: oa.Audio, b: oa.Audio, gainA: float = ..., gainB: float = ...) -> oa.Audio:
	"""
	Weighted sum of two audio values with identical rate, layout, shape, and dtype.
	"""

def normalize(audio: oa.Audio, targetDb: float = ..., mode: int = ...) -> oa.Audio:
	"""
	Peak or RMS normalization to a target dB level while preserving audio metadata.
	"""

def preEmphasis(audio: oa.Audio, alpha: float = ...) -> oa.Audio:
	"""Apply y[n] = x[n] - alpha*x[n-1] while preserving audio metadata."""

def resample(audio: oa.Audio, outRate: int = ..., filterHalfWidth: int = ...) -> oa.Audio:
	"""
	Windowed-sinc resampling with sample rate derived from the input audio value.
	"""

def saturate(audio: oa.Audio, driveDb: float = ..., mix: float = ...) -> oa.Audio:
	"""
	Apply a tanh soft-clipping waveshaper with drive in dB and a dry/wet mix.
	"""

def sosFilter(audio: oa.Audio, sections: Sequence[oa.BiquadCoefficients]) -> oa.Audio:
	"""
	Apply one to 64 stable a0-normalized second-order sections as a zero-state cascade.
	"""

def stft(audio: oa.Audio, cfg: oa.StftConfig = ...) -> oa.Matrix:
	"""Short-time Fourier magnitude transform to [Channels, Frames, Bins]."""

def toMono(audio: oa.Audio) -> oa.Audio:
	"""
	Average channels into one mono channel while retaining the sample rate.
	"""

def waveformEnvelope(audio: oa.Audio, bins: int = ...) -> oa.Matrix:
	"""Reduce multichannel audio to a peak-preserving min/max display matrix."""
