// AUTO-GENERATED by tools/gen/fn/generate.py — DO NOT EDIT.
// Source of truth: tools/gen/fn/schema/**.toml.
// Included inside the owning bind function.

	m.def("clipReward", [](const oa::Matrix& inReward, oa::F32 inMinimum, oa::F32 inMaximum) {
		auto result = oa::FnEnvironment::clipReward(inReward, inMinimum, inMaximum);
		if (result.isEmpty()) {
			throw std::runtime_error("ClipReward rejected its input");
		}
		return matrixPtr(oa::move(result));
	},
	  nb::arg("reward"), nb::arg("minimum") = -1.0F, nb::arg("maximum") = 1.0F,
	  nb::rv_policy::take_ownership,
	  "Clip rewards to a closed interval.");

	m.def("normalizeObservation", [](const oa::Matrix& inObservation, const oa::Matrix& inMean, const oa::Matrix& inStddev, oa::F32 inEpsilon, oa::F32 inClip) {
		auto result = oa::FnEnvironment::normalizeObservation(inObservation, inMean, inStddev, inEpsilon, inClip);
		if (result.isEmpty()) {
			throw std::runtime_error("NormalizeObservation rejected its input");
		}
		return matrixPtr(oa::move(result));
	},
	  nb::arg("observation"), nb::arg("mean"), nb::arg("stddev"), nb::arg("epsilon") = 1.0e-6F, nb::arg("clip") = 10.0F,
	  nb::rv_policy::take_ownership,
	  "Normalize and clip observations with broadcast statistics.");

	m.def("scaleAction", [](const oa::Matrix& inAction, oa::F32 inSourceMinimum, oa::F32 inSourceMaximum, oa::F32 inTargetMinimum, oa::F32 inTargetMaximum, bool inClamp) {
		auto result = oa::FnEnvironment::scaleAction(inAction, inSourceMinimum, inSourceMaximum, inTargetMinimum, inTargetMaximum, inClamp);
		if (result.isEmpty()) {
			throw std::runtime_error("ScaleAction rejected its input");
		}
		return matrixPtr(oa::move(result));
	},
	  nb::arg("action"), nb::arg("sourceMinimum"), nb::arg("sourceMaximum"), nb::arg("targetMinimum"), nb::arg("targetMaximum"), nb::arg("clamp") = true,
	  nb::rv_policy::take_ownership,
	  "Map an action tensor between bounded intervals.");
