// AUTO-GENERATED by tools/gen/fn/generate.py — DO NOT EDIT.
// Source of truth: tools/gen/fn/schema/**.toml.
// Included inside the owning bind function.

	m.def("bce", [](const oa::Matrix& inA, const oa::Matrix& inB) {
		return matrixPtr(oa::FnLoss::bce(inA, inB));
	},
	  nb::arg("a"), nb::arg("b"),
	  nb::rv_policy::take_ownership,
	  "Compute mean binary cross entropy over probabilities.");

	m.def("bceBwd", [](const oa::Matrix& inA, const oa::Matrix& inB) {
		return matrixPtr(oa::FnLoss::bceBwd(inA, inB));
	},
	  nb::arg("a"), nb::arg("b"),
	  nb::rv_policy::take_ownership,
	  "Compute the explicit BCE gradient with respect to A.");

	m.def("crossEntropy", [](const oa::Matrix& inLogits, const oa::Matrix& inTargets) {
		return matrixPtr(oa::FnLoss::crossEntropy(inLogits, inTargets));
	},
	  nb::arg("logits"), nb::arg("targets"),
	  nb::rv_policy::take_ownership,
	  "Mean cross-entropy for rank-two logits and UInt8, UInt32, or non-negative Int32 class-index targets; an out-of-range target produces NaN without an out-of-bounds read.");

	m.def("crossEntropyBwd", [](const oa::Matrix& inLogits, const oa::Matrix& inTargets) {
		return matrixPtr(oa::FnLoss::crossEntropyBwd(inLogits, inTargets));
	},
	  nb::arg("logits"), nb::arg("targets"),
	  nb::rv_policy::take_ownership,
	  "Compute the explicit cross-entropy gradient with respect to logits.");

	m.def("dqn", [](const oa::Matrix& inQ, const oa::Matrix& inAction, const oa::Matrix& inReward, const oa::Matrix& inNextQ, const oa::Matrix& inTerminated, const oa::Matrix& inTruncated, const oa::DqnLossConfig& inConfig) {
		auto result = oa::FnLoss::dqn(inQ, inAction, inReward, inNextQ, inTerminated, inTruncated, inConfig);
		if (not result.isValid()) {
			throw std::runtime_error("Dqn rejected its input");
		}
		return new oa::DqnLossResult(std::move(result));
	},
	  nb::arg("q"), nb::arg("action"), nb::arg("reward"), nb::arg("nextQ"), nb::arg("terminated"), nb::arg("truncated"), nb::arg("config") = oa::DqnLossConfig{},
	  nb::rv_policy::take_ownership,
	  "Compute the one-step DQN target and temporal-difference loss.");

	m.def("l1", [](const oa::Matrix& inA, const oa::Matrix& inB) {
		return matrixPtr(oa::FnLoss::l1(inA, inB));
	},
	  nb::arg("a"), nb::arg("b"),
	  nb::rv_policy::take_ownership,
	  "Compute mean absolute error.");

	m.def("l1Bwd", [](const oa::Matrix& inA, const oa::Matrix& inB) {
		return matrixPtr(oa::FnLoss::l1Bwd(inA, inB));
	},
	  nb::arg("a"), nb::arg("b"),
	  nb::rv_policy::take_ownership,
	  "Compute the explicit L1 gradient with respect to A.");

	m.def("mse", [](const oa::Matrix& inA, const oa::Matrix& inB) {
		return matrixPtr(oa::FnLoss::mse(inA, inB));
	},
	  nb::arg("a"), nb::arg("b"),
	  nb::rv_policy::take_ownership,
	  "Compute mean squared error.");

	m.def("mseBwd", [](const oa::Matrix& inA, const oa::Matrix& inB) {
		return matrixPtr(oa::FnLoss::mseBwd(inA, inB));
	},
	  nb::arg("a"), nb::arg("b"),
	  nb::rv_policy::take_ownership,
	  "Compute the explicit MSE gradient with respect to A.");

	m.def("ppo", [](const oa::Matrix& inNewLogProbability, const oa::Matrix& inOldLogProbability, const oa::Matrix& inAdvantage, const oa::Matrix& inValue, const oa::Matrix& inTargetReturn, const oa::Matrix& inEntropy, const oa::PpoLossConfig& inConfig) {
		auto result = oa::FnLoss::ppo(inNewLogProbability, inOldLogProbability, inAdvantage, inValue, inTargetReturn, inEntropy, inConfig);
		if (not result.isValid()) {
			throw std::runtime_error("Ppo rejected its input");
		}
		return new oa::PpoLossResult(std::move(result));
	},
	  nb::arg("newLogProbability"), nb::arg("oldLogProbability"), nb::arg("advantage"), nb::arg("value"), nb::arg("targetReturn"), nb::arg("entropy"), nb::arg("config") = oa::PpoLossConfig{},
	  nb::rv_policy::take_ownership,
	  "Compose PPO policy, value, entropy, and total losses.");

	m.def("ppoClippedPolicy", [](const oa::Matrix& inNewLogProbability, const oa::Matrix& inOldLogProbability, const oa::Matrix& inAdvantage, oa::F32 inClipEpsilon) {
		auto result = oa::FnLoss::ppoClippedPolicy(inNewLogProbability, inOldLogProbability, inAdvantage, inClipEpsilon);
		if (result.isEmpty()) {
			throw std::runtime_error("PpoClippedPolicy rejected its input");
		}
		return matrixPtr(std::move(result));
	},
	  nb::arg("newLogProbability"), nb::arg("oldLogProbability"), nb::arg("advantage"), nb::arg("clipEpsilon") = 0.2F,
	  nb::rv_policy::take_ownership,
	  "Compute the clipped PPO policy objective.");

	m.def("ppoClippedPolicyBwd", [](const oa::Matrix& inNewLogProbability, const oa::Matrix& inOldLogProbability, const oa::Matrix& inAdvantage, oa::F32 inClipEpsilon) {
		auto result = oa::FnLoss::ppoClippedPolicyBwd(inNewLogProbability, inOldLogProbability, inAdvantage, inClipEpsilon);
		if (result.isEmpty()) {
			throw std::runtime_error("PpoClippedPolicyBwd rejected its input");
		}
		return matrixPtr(std::move(result));
	},
	  nb::arg("newLogProbability"), nb::arg("oldLogProbability"), nb::arg("advantage"), nb::arg("clipEpsilon") = 0.2F,
	  nb::rv_policy::take_ownership,
	  "Compute the explicit clipped PPO gradient with respect to new log probability.");

	m.def("sacActor", [](const oa::Matrix& inQ1, const oa::Matrix& inQ2, const oa::Matrix& inLogProbability, oa::F32 inEntropyCoefficient) {
		auto result = oa::FnLoss::sacActor(inQ1, inQ2, inLogProbability, inEntropyCoefficient);
		if (result.isEmpty()) {
			throw std::runtime_error("SacActor rejected its input");
		}
		return matrixPtr(std::move(result));
	},
	  nb::arg("q1"), nb::arg("q2"), nb::arg("logProbability"), nb::arg("entropyCoefficient") = 0.2F,
	  nb::rv_policy::take_ownership,
	  "Compute the entropy-regularized SAC actor loss.");

	m.def("sacCritic", [](const oa::Matrix& inQ1, const oa::Matrix& inQ2, const oa::Matrix& inReward, const oa::Matrix& inNextQ1, const oa::Matrix& inNextQ2, const oa::Matrix& inNextLogProbability, const oa::Matrix& inTerminated, const oa::Matrix& inTruncated, const oa::SacLossConfig& inConfig) {
		auto result = oa::FnLoss::sacCritic(inQ1, inQ2, inReward, inNextQ1, inNextQ2, inNextLogProbability, inTerminated, inTruncated, inConfig);
		if (not result.isValid()) {
			throw std::runtime_error("SacCritic rejected its input");
		}
		return new oa::SacCriticLossResult(std::move(result));
	},
	  nb::arg("q1"), nb::arg("q2"), nb::arg("reward"), nb::arg("nextQ1"), nb::arg("nextQ2"), nb::arg("nextLogProbability"), nb::arg("terminated"), nb::arg("truncated"), nb::arg("config") = oa::SacLossConfig{},
	  nb::rv_policy::take_ownership,
	  "Compute detached SAC critic targets and twin critic losses.");

	m.def("smoothL1", [](const oa::Matrix& inA, const oa::Matrix& inB) {
		return matrixPtr(oa::FnLoss::smoothL1(inA, inB));
	},
	  nb::arg("a"), nb::arg("b"),
	  nb::rv_policy::take_ownership,
	  "Compute mean smooth-L1 loss with unit beta.");

	m.def("smoothL1Bwd", [](const oa::Matrix& inA, const oa::Matrix& inB) {
		return matrixPtr(oa::FnLoss::smoothL1Bwd(inA, inB));
	},
	  nb::arg("a"), nb::arg("b"),
	  nb::rv_policy::take_ownership,
	  "Compute the explicit smooth-L1 gradient with respect to A.");
