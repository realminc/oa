// Camera capture belongs to Vision. Include this transitional path only to
// reach the canonical camera API; it declares no duplicate UI surface.

#pragma once

#include <oa/vision/cameraCapture.h>
