// AUTO-GENERATED by tools/nnAutogen/oannautogen.py - DO NOT EDIT.
// source of truth: tools/nnAutogen/schema/ml/mlNnVisionAdvanced.toml.
// Regenerate via: python3 tools/nnAutogen/oannautogen.py --live
// oa::AdaptiveAvgPool2d - Adaptive average pooling layer.

#include <oa/ml/nn.h>
#include <oa/core/fnMatrix.h>
#include <oa/core/validation.h>

oa::AdaptiveAvgPool2d::AdaptiveAvgPool2d(oa::I32 inOutputSize)
	: outputSize_(inOutputSize) {

}

oa::Matrix oa::AdaptiveAvgPool2d::forward(const oa::Matrix& inInput) {
	auto shape = inInput.getShape();
	oa::I32 h = shape[2];
	oa::I32 w = shape[3];

	// For global average pooling (outputSize=1)
	if (outputSize_ == 1)
	{
		return oa::FnMatrix::avgPool2d(inInput, h, 1, 0);
	}

	// For general adaptive pooling, compute stride and kernel size
	oa::I32 strideH = h / outputSize_;
	oa::I32 strideW = w / outputSize_;
	oa::I32 kernelH = h - (outputSize_ - 1) * strideH;

	// Use avgPool2d with computed kernel/stride
	return oa::FnMatrix::avgPool2d(inInput, kernelH, strideH, 0);
}
