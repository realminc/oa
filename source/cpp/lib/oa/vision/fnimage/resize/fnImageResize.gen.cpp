// AUTO-GENERATED by tools/fnAutogen/oafnautogen.py — DO NOT EDIT.
// Source of truth: tools/fnAutogen/schema/vision/visionFnImageResize.toml.
// Regenerate via: python3 tools/fnAutogen/oafnautogen.py --live
// oa::FnImage — Resize GPU operations.
// Unified API: records operations to oa::ExecutionSession.

#include <oa/core/matrix.h>
#include <oa/core/fnMatrix.h>
#include <oa/core/op.h>
#include <oa/vision/fnImage.h>
#include <oa/core/status.h>
#include <oa/core/types.h>
#include <oa/core/bufferAccess.h>
#include <oa/runtime/executionSession.h>
#include <oa/core/validation.h>

// ─── resize (Resize an NCHW image using nearest or bilinear interpolation) ─

oa::Matrix oa::FnImage::resize(const oa::Matrix& inImage, oa::U32 inTargetWidth, oa::U32 inTargetHeight, oa::InterpolationMode inMode) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::resize(session.engine(), inImage, inTargetWidth, inTargetHeight, inMode);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::resize, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("targetWidth", inTargetWidth), oa::OpAttribute::fromUnsignedInteger("targetHeight", inTargetHeight), oa::OpAttribute::fromUnsignedInteger("interpolation", static_cast<oa::U64>(inMode))});
	if (not status.isOk()) return {};
	return out;
}
