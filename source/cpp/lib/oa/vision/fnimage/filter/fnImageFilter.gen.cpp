// AUTO-GENERATED by tools/gen/fn/generate.py — DO NOT EDIT.
// Source of truth: tools/gen/fn/schema/vision/visionFnImageFilter.toml.
// Regenerate via: python3 tools/gen/fn/generate.py --live
// oa::FnImage — Filter GPU operations.
// Unified API: records operations to oa::ExecutionSession.

#include <oa/core/matrix.h>
#include <oa/core/fnMatrix.h>
#include <oa/core/op.h>
#include <oa/vision/fnImage.h>
#include <oa/core/status.h>
#include <oa/core/types.h>
#include <oa/core/bufferAccess.h>
#include <oa/runtime/executionSession.h>
#include <oa/core/validation.h>

// ─── convolve2d (Same-shape NCHW 2D cross-correlation with an odd shared ker

oa::Matrix oa::FnImage::convolve2d(const oa::Matrix& inImage, const oa::Matrix& inKernel, oa::BorderMode inBorder, oa::F32 inBorderValue) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::convolve2d(session.engine(), inImage, inKernel, inBorder, inBorderValue);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::convolve2d, {&inImage, &inKernel}, {&out}, {oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder)), oa::OpAttribute::fromFloat("borderValue", inBorderValue)});
	if (not status.isOk()) return {};
	return out;
}

// ─── separableConvolve2d (Same-shape NCHW separable cross-correlation with o

oa::Matrix oa::FnImage::separableConvolve2d(const oa::Matrix& inImage, const oa::Matrix& inKernelX, const oa::Matrix& inKernelY, oa::BorderMode inBorder, oa::F32 inBorderValue) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::separableConvolve2d(session.engine(), inImage, inKernelX, inKernelY, inBorder, inBorderValue);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::separableConvolve2d, {&inImage, &inKernelX, &inKernelY}, {&out}, {oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder)), oa::OpAttribute::fromFloat("borderValue", inBorderValue)});
	if (not status.isOk()) return {};
	return out;
}

// ─── averageBlur (Normalized box blur lowered through the generic 2D filter)

oa::Matrix oa::FnImage::averageBlur(const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::averageBlur(session.engine(), inImage, inKernelWidth, inKernelHeight, inBorder);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::averageBlur, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("kernelWidth", inKernelWidth), oa::OpAttribute::fromUnsignedInteger("kernelHeight", inKernelHeight), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder))});
	if (not status.isOk()) return {};
	return out;
}

// ─── sobel (Signed 3x3 Sobel x or y derivative lowered through the generic 2

oa::Matrix oa::FnImage::sobel(const oa::Matrix& inImage, oa::U32 inDx, oa::U32 inDy, oa::BorderMode inBorder) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::sobel(session.engine(), inImage, inDx, inDy, inBorder);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::sobel, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("dx", inDx), oa::OpAttribute::fromUnsignedInteger("dy", inDy), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder))});
	if (not status.isOk()) return {};
	return out;
}

// ─── scharr (Signed 3x3 Scharr x or y derivative lowered through the generic

oa::Matrix oa::FnImage::scharr(const oa::Matrix& inImage, oa::U32 inDx, oa::U32 inDy, oa::BorderMode inBorder) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::scharr(session.engine(), inImage, inDx, inDy, inBorder);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::scharr, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("dx", inDx), oa::OpAttribute::fromUnsignedInteger("dy", inDy), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder))});
	if (not status.isOk()) return {};
	return out;
}

// ─── laplacian (Signed four-neighbour discrete Laplacian lowered through the

oa::Matrix oa::FnImage::laplacian(const oa::Matrix& inImage, oa::BorderMode inBorder) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::laplacian(session.engine(), inImage, inBorder);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::laplacian, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder))});
	if (not status.isOk()) return {};
	return out;
}

// ─── erode (Rectangular NCHW erosion using the shared border contract) ────

oa::Matrix oa::FnImage::erode(const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::erode(session.engine(), inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::erode, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("kernelWidth", inKernelWidth), oa::OpAttribute::fromUnsignedInteger("kernelHeight", inKernelHeight), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder)), oa::OpAttribute::fromFloat("borderValue", inBorderValue)});
	if (not status.isOk()) return {};
	return out;
}

// ─── dilate (Rectangular NCHW dilation using the shared border contract) ───

oa::Matrix oa::FnImage::dilate(const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::dilate(session.engine(), inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::dilate, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("kernelWidth", inKernelWidth), oa::OpAttribute::fromUnsignedInteger("kernelHeight", inKernelHeight), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder)), oa::OpAttribute::fromFloat("borderValue", inBorderValue)});
	if (not status.isOk()) return {};
	return out;
}

// ─── morphologyOpen (Erosion followed by dilation through the reusable morph

oa::Matrix oa::FnImage::morphologyOpen(const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::morphologyOpen(session.engine(), inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::morphologyOpen, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("kernelWidth", inKernelWidth), oa::OpAttribute::fromUnsignedInteger("kernelHeight", inKernelHeight), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder)), oa::OpAttribute::fromFloat("borderValue", inBorderValue)});
	if (not status.isOk()) return {};
	return out;
}

// ─── morphologyClose (Dilation followed by erosion through the reusable morp

oa::Matrix oa::FnImage::morphologyClose(const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::morphologyClose(session.engine(), inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::morphologyClose, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("kernelWidth", inKernelWidth), oa::OpAttribute::fromUnsignedInteger("kernelHeight", inKernelHeight), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder)), oa::OpAttribute::fromFloat("borderValue", inBorderValue)});
	if (not status.isOk()) return {};
	return out;
}

// ─── morphologyGradient (Difference between dilation and erosion) ────

oa::Matrix oa::FnImage::morphologyGradient(const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::morphologyGradient(session.engine(), inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::morphologyGradient, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("kernelWidth", inKernelWidth), oa::OpAttribute::fromUnsignedInteger("kernelHeight", inKernelHeight), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder)), oa::OpAttribute::fromFloat("borderValue", inBorderValue)});
	if (not status.isOk()) return {};
	return out;
}

// ─── gaussianBlur (Gaussian blur filter with configurable kernel size and si

oa::Matrix oa::FnImage::gaussianBlur(const oa::Matrix& inImage, oa::F32 inSigma, oa::U32 inKernelSize) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::gaussianBlur(session.engine(), inImage, inSigma, inKernelSize);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::gaussianBlur, {&inImage}, {&out}, {oa::OpAttribute::fromFloat("sigma", inSigma), oa::OpAttribute::fromUnsignedInteger("kernelSize", inKernelSize)});
	if (not status.isOk()) return {};
	return out;
}

// ─── sharpen (Parameterized 3x3 sharpening lowered through Convolve2d) ────

oa::Matrix oa::FnImage::sharpen(const oa::Matrix& inImage, oa::F32 inAmount, oa::BorderMode inBorder) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::sharpen(session.engine(), inImage, inAmount, inBorder);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::sharpen, {&inImage}, {&out}, {oa::OpAttribute::fromFloat("amount", inAmount), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder))});
	if (not status.isOk()) return {};
	return out;
}

// ─── medianBlur (Odd-window median filter with explicit border semantics) ──

oa::Matrix oa::FnImage::medianBlur(const oa::Matrix& inImage, oa::U32 inKernelSize, oa::BorderMode inBorder) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::medianBlur(session.engine(), inImage, inKernelSize, inBorder);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::medianBlur, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("kernelSize", inKernelSize), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder))});
	if (not status.isOk()) return {};
	return out;
}

// ─── bilateralFilter (Edge-preserving bilateral neighborhood filter) ────

oa::Matrix oa::FnImage::bilateralFilter(const oa::Matrix& inImage, oa::U32 inKernelSize, oa::F32 inSigmaColor, oa::F32 inSigmaSpace, oa::BorderMode inBorder) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::bilateralFilter(session.engine(), inImage, inKernelSize, inSigmaColor, inSigmaSpace, inBorder);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::bilateralFilter, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("kernelSize", inKernelSize), oa::OpAttribute::fromFloat("sigmaColor", inSigmaColor), oa::OpAttribute::fromFloat("sigmaSpace", inSigmaSpace), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder))});
	if (not status.isOk()) return {};
	return out;
}

// ─── unsharpMask (Gaussian unsharp masking composed from graph-native primit

oa::Matrix oa::FnImage::unsharpMask(const oa::Matrix& inImage, oa::F32 inSigma, oa::F32 inAmount, oa::U32 inKernelSize) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::unsharpMask(session.engine(), inImage, inSigma, inAmount, inKernelSize);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::unsharpMask, {&inImage}, {&out}, {oa::OpAttribute::fromFloat("sigma", inSigma), oa::OpAttribute::fromFloat("amount", inAmount), oa::OpAttribute::fromUnsignedInteger("kernelSize", inKernelSize)});
	if (not status.isOk()) return {};
	return out;
}

// ─── morphologyTopHat (Input minus morphological opening) ────

oa::Matrix oa::FnImage::morphologyTopHat(const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::morphologyTopHat(session.engine(), inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::morphologyTopHat, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("kernelWidth", inKernelWidth), oa::OpAttribute::fromUnsignedInteger("kernelHeight", inKernelHeight), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder)), oa::OpAttribute::fromFloat("borderValue", inBorderValue)});
	if (not status.isOk()) return {};
	return out;
}

// ─── morphologyBlackHat (Morphological closing minus input) ────

oa::Matrix oa::FnImage::morphologyBlackHat(const oa::Matrix& inImage, oa::U32 inKernelWidth, oa::U32 inKernelHeight, oa::BorderMode inBorder, oa::F32 inBorderValue) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::morphologyBlackHat(session.engine(), inImage, inKernelWidth, inKernelHeight, inBorder, inBorderValue);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::morphologyBlackHat, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("kernelWidth", inKernelWidth), oa::OpAttribute::fromUnsignedInteger("kernelHeight", inKernelHeight), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder)), oa::OpAttribute::fromFloat("borderValue", inBorderValue)});
	if (not status.isOk()) return {};
	return out;
}

// ─── adaptiveThresholdMean (Local mean adaptive binary threshold) ────

oa::Matrix oa::FnImage::adaptiveThresholdMean(const oa::Matrix& inImage, oa::U32 inKernelSize, oa::F32 inC, oa::F32 inMaxValue, oa::BorderMode inBorder) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::adaptiveThresholdMean(session.engine(), inImage, inKernelSize, inC, inMaxValue, inBorder);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::adaptiveThresholdMean, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("kernelSize", inKernelSize), oa::OpAttribute::fromFloat("c", inC), oa::OpAttribute::fromFloat("maxValue", inMaxValue), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder))});
	if (not status.isOk()) return {};
	return out;
}

// ─── adaptiveThresholdGaussian (Local Gaussian adaptive binary threshold) ──

oa::Matrix oa::FnImage::adaptiveThresholdGaussian(const oa::Matrix& inImage, oa::U32 inKernelSize, oa::F32 inC, oa::F32 inMaxValue, oa::F32 inSigma, oa::BorderMode inBorder) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::adaptiveThresholdGaussian(session.engine(), inImage, inKernelSize, inC, inMaxValue, inSigma, inBorder);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::adaptiveThresholdGaussian, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("kernelSize", inKernelSize), oa::OpAttribute::fromFloat("c", inC), oa::OpAttribute::fromFloat("maxValue", inMaxValue), oa::OpAttribute::fromFloat("sigma", inSigma), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder))});
	if (not status.isOk()) return {};
	return out;
}
