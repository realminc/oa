// AUTO-GENERATED by tools/fnAutogen/oafnautogen.py — DO NOT EDIT.
// Source of truth: tools/fnAutogen/schema/vision/visionFnImageGeometric.toml.
// Regenerate via: python3 tools/fnAutogen/oafnautogen.py --live
// oa::FnImage — Geometric GPU operations.
// Unified API: records operations to oa::ExecutionSession.

#include <oa/core/matrix.h>
#include <oa/core/fnMatrix.h>
#include <oa/core/op.h>
#include <oa/vision/fnImage.h>
#include <oa/core/status.h>
#include <oa/core/types.h>
#include <oa/core/bufferAccess.h>
#include <oa/runtime/executionSession.h>
#include <oa/core/validation.h>

// ─── crop (Crop NCHW image: out = A[y:y+h, x:x+w]) ────

oa::Matrix oa::FnImage::crop(const oa::Matrix& inImage, oa::U32 inX, oa::U32 inY, oa::U32 inWidth, oa::U32 inHeight) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::crop(session.engine(), inImage, inX, inY, inWidth, inHeight);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::crop, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("x", inX), oa::OpAttribute::fromUnsignedInteger("y", inY), oa::OpAttribute::fromUnsignedInteger("width", inWidth), oa::OpAttribute::fromUnsignedInteger("height", inHeight)});
	if (not status.isOk()) return {};
	return out;
}

// ─── flip (Flip NCHW image horizontally or vertically) ────

oa::Matrix oa::FnImage::flip(const oa::Matrix& inImage, bool inHorizontal, bool inVertical) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::flip(session.engine(), inImage, inHorizontal, inVertical);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::flip, {&inImage}, {&out}, {oa::OpAttribute::fromBoolean("horizontal", inHorizontal), oa::OpAttribute::fromBoolean("vertical", inVertical)});
	if (not status.isOk()) return {};
	return out;
}

// ─── rotate (Rotate NCHW image by 90/180/270 degrees) ────

oa::Matrix oa::FnImage::rotate(const oa::Matrix& inImage, oa::U32 inDegrees) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::rotate(session.engine(), inImage, inDegrees);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::rotate, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("degrees", inDegrees)});
	if (not status.isOk()) return {};
	return out;
}

// ─── pad (Asymmetric NCHW padding with the shared border contract) ────

oa::Matrix oa::FnImage::pad(const oa::Matrix& inImage, oa::U32 inLeft, oa::U32 inRight, oa::U32 inTop, oa::U32 inBottom, oa::BorderMode inBorder, oa::F32 inBorderValue) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::pad(session.engine(), inImage, inLeft, inRight, inTop, inBottom, inBorder, inBorderValue);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::pad, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("left", inLeft), oa::OpAttribute::fromUnsignedInteger("right", inRight), oa::OpAttribute::fromUnsignedInteger("top", inTop), oa::OpAttribute::fromUnsignedInteger("bottom", inBottom), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder)), oa::OpAttribute::fromFloat("borderValue", inBorderValue)});
	if (not status.isOk()) return {};
	return out;
}

// ─── centerCrop (Centered crop lowered through Crop) ────

oa::Matrix oa::FnImage::centerCrop(const oa::Matrix& inImage, oa::U32 inWidth, oa::U32 inHeight) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::centerCrop(session.engine(), inImage, inWidth, inHeight);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::centerCrop, {&inImage}, {&out}, {oa::OpAttribute::fromUnsignedInteger("width", inWidth), oa::OpAttribute::fromUnsignedInteger("height", inHeight)});
	if (not status.isOk()) return {};
	return out;
}

// ─── remap (Absolute-coordinate NCHW remapping with nearest or bilinear samp

oa::Matrix oa::FnImage::remap(const oa::Matrix& inImage, const oa::Matrix& inMap, oa::InterpolationMode inInterpolation, oa::BorderMode inBorder, oa::F32 inBorderValue) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::remap(session.engine(), inImage, inMap, inInterpolation, inBorder, inBorderValue);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::remap, {&inImage, &inMap}, {&out}, {oa::OpAttribute::fromUnsignedInteger("interpolation", static_cast<oa::U64>(inInterpolation)), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder)), oa::OpAttribute::fromFloat("borderValue", inBorderValue)});
	if (not status.isOk()) return {};
	return out;
}

// ─── warpAffine (Inverse 2x3 affine warp with explicit interpolation and bor

oa::Matrix oa::FnImage::warpAffine(const oa::Matrix& inImage, const oa::Matrix& inTransform, oa::U32 inWidth, oa::U32 inHeight, oa::InterpolationMode inInterpolation, oa::BorderMode inBorder, oa::F32 inBorderValue) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::warpAffine(session.engine(), inImage, inTransform, inWidth, inHeight, inInterpolation, inBorder, inBorderValue);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::warpAffine, {&inImage, &inTransform}, {&out}, {oa::OpAttribute::fromUnsignedInteger("width", inWidth), oa::OpAttribute::fromUnsignedInteger("height", inHeight), oa::OpAttribute::fromUnsignedInteger("interpolation", static_cast<oa::U64>(inInterpolation)), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder)), oa::OpAttribute::fromFloat("borderValue", inBorderValue)});
	if (not status.isOk()) return {};
	return out;
}

// ─── warpPerspective (Inverse 3x3 perspective warp with explicit interpolati

oa::Matrix oa::FnImage::warpPerspective(const oa::Matrix& inImage, const oa::Matrix& inTransform, oa::U32 inWidth, oa::U32 inHeight, oa::InterpolationMode inInterpolation, oa::BorderMode inBorder, oa::F32 inBorderValue) {
	auto& session = oa::ExecutionSession::getActive();
	oa::OpLoweringScope lowering(session);
	auto out = oa::FnImage::warpPerspective(session.engine(), inImage, inTransform, inWidth, inHeight, inInterpolation, inBorder, inBorderValue);
	const auto status = lowering.commit(
		oa::detail::opRegistry::FnImage::warpPerspective, {&inImage, &inTransform}, {&out}, {oa::OpAttribute::fromUnsignedInteger("width", inWidth), oa::OpAttribute::fromUnsignedInteger("height", inHeight), oa::OpAttribute::fromUnsignedInteger("interpolation", static_cast<oa::U64>(inInterpolation)), oa::OpAttribute::fromUnsignedInteger("border", static_cast<oa::U64>(inBorder)), oa::OpAttribute::fromFloat("borderValue", inBorderValue)});
	if (not status.isOk()) return {};
	return out;
}
