# AUTO-GENERATED by Tools/Python/oapystubgen.py — DO NOT EDIT.
# Sources of truth: the live nanobind signatures and oa/_surface.py.

"""OA audio codecs and GPU signal operations"""

import oa
from oa import (
	OaAudio as OaAudio,
	OaAudioDecoder as OaAudioDecoder,
	OaAudioEncoder as OaAudioEncoder,
	OaChannelLayout as OaChannelLayout,
	OaMelConfig as OaMelConfig,
	OaMfccConfig as OaMfccConfig,
	OaNormalizeAudioConfig as OaNormalizeAudioConfig,
	OaResampleConfig as OaResampleConfig,
	OaStftConfig as OaStftConfig
)


def OaChannelsForLayout(Layout: oa.OaChannelLayout) -> int:
	"""Expected channel count for a layout (0 = unknown)."""

def OaLayoutForChannels(Channels: int) -> oa.OaChannelLayout:
	"""Best-effort layout for a raw channel count."""

def Stft(Audio: oa.OaAudio, Config: oa.OaStftConfig = ...) -> oa.OaMatrix:
	"""STFT magnitude → [Channels, Frames, FftSize/2+1] F32."""

def MelSpectrogram(Audio: oa.OaAudio, Config: oa.OaMelConfig = ...) -> oa.OaMatrix:
	"""
	Mel spectrogram → [Channels, NumMels, Frames] F32 (Whisper/CLAP layout).
	"""

def Mfcc(Audio: oa.OaAudio, Config: oa.OaMfccConfig = ...) -> oa.OaMatrix:
	"""
	MFCC → [Channels, NumCoeffs, Frames] F32 (orthonormal DCT-II of log-mel).
	"""

def Normalize(Audio: oa.OaAudio, TargetDb: float = ..., Mode: int = ...) -> oa.OaAudio:
	"""Peak (mode 0) or RMS (mode 1) normalization to target_db."""

def Resample(Audio: oa.OaAudio, OutRate: int = ..., FilterHalfWidth: int = ...) -> oa.OaAudio:
	"""Windowed-sinc resample → [Channels, Samples·out/in] F32."""

def Gain(Audio: oa.OaAudio, GainDb: float) -> oa.OaAudio:
	"""Scalar gain in dB."""

def Clip(Audio: oa.OaAudio, Min: float = ..., Max: float = ...) -> oa.OaAudio:
	"""Hard clip to [min, max]."""

def AmplitudeToDb(Audio: oa.OaAudio, FloorDb: float = ...) -> oa.OaMatrix:
	"""20·log10(max(|x|, floor)) — amplitude to dB with a silence floor."""

def PreEmphasis(Audio: oa.OaAudio, Alpha: float = ...) -> oa.OaAudio:
	"""Pre-emphasis filter y[n] = x[n] − α·x[n−1] (y[0] = x[0])."""

def ToMono(Audio: oa.OaAudio) -> oa.OaAudio:
	"""Average channels → [1, Samples]."""

def Fade(Audio: oa.OaAudio, FadeInSamples: int, FadeOutSamples: int) -> oa.OaAudio:
	"""Linear fade-in/out envelope (sample counts per edge)."""

def Mix(A: oa.OaAudio, B: oa.OaAudio, GainA: float = ..., GainB: float = ...) -> oa.OaAudio:
	"""Weighted sum gain_a·a + gain_b·b."""
