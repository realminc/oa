#pragma once

#include <Oa/Core/FnMatrix.h>

// Compatibility header retained for existing includes. UI presentation is
// exposed through OaUi; it does not currently add public OaFnMatrix functions.
