// AUTO-GENERATED by Tools/FnAutogen/oafnautogen.py — DO NOT EDIT.
// Source of truth: Tools/FnAutogen/Schema/Core/CoreFnMatrixElemwise.toml.
// Regenerate via: python3 Tools/FnAutogen/oafnautogen.py --live

// OaFnMatrix — Elemwise overloads.

	/// Scale: Scalar multiplication: Out = A * Scalar.
	/// Context-based: auto y = OaFnMatrix::Scale(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Scale(const OaMatrix& InA, OaF32 InScalar);

	/// Neg: Element-wise negation: Out = -A.
	/// Context-based: auto y = OaFnMatrix::Neg(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Neg(const OaMatrix& InA);

	/// Abs: Element-wise absolute value: Out = |A|.
	/// Context-based: auto y = OaFnMatrix::Abs(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Abs(const OaMatrix& InA);

	/// Log: Element-wise natural log: Out = log(A).
	/// Context-based: auto y = OaFnMatrix::Log(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Log(const OaMatrix& InA);

	/// Sqrt: Element-wise square root: Out = sqrt(A).
	/// Context-based: auto y = OaFnMatrix::Sqrt(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Sqrt(const OaMatrix& InA);

	/// Pow: Element-wise power: Out = A^Exponent.
	/// Context-based: auto y = OaFnMatrix::Pow(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Pow(const OaMatrix& InA, OaF32 InExponent);

	/// AddScalar: Scalar addition: Out = A + Scalar.
	/// Context-based: auto y = OaFnMatrix::AddScalar(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix AddScalar(const OaMatrix& InA, OaF32 InScalar);

	/// SubScalar: Scalar subtraction: Out = A - Scalar.
	/// Context-based: auto y = OaFnMatrix::SubScalar(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix SubScalar(const OaMatrix& InA, OaF32 InScalar);

	/// DivScalar: Scalar division: Out = A / Scalar.
	/// Context-based: auto y = OaFnMatrix::DivScalar(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix DivScalar(const OaMatrix& InA, OaF32 InScalar);

	/// Exp: Element-wise exponential: Out = exp(A).
	/// Context-based: auto y = OaFnMatrix::Exp(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Exp(const OaMatrix& InA);

	/// Sin: Element-wise sine: Out = sin(A).
	/// Context-based: auto y = OaFnMatrix::Sin(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Sin(const OaMatrix& InA);

	/// Cos: Element-wise cosine: Out = cos(A).
	/// Context-based: auto y = OaFnMatrix::Cos(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Cos(const OaMatrix& InA);

	/// Reciprocal: Element-wise reciprocal: Out = 1 / A.
	/// Context-based: auto y = OaFnMatrix::Reciprocal(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Reciprocal(const OaMatrix& InA);

	/// ClampMax: Element-wise clamp max: Out = min(A, Max).
	/// Context-based: auto y = OaFnMatrix::ClampMax(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix ClampMax(const OaMatrix& InA, OaF32 InMax);

	/// ClampMin: Element-wise clamp min: Out = max(A, Min).
	/// Context-based: auto y = OaFnMatrix::ClampMin(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix ClampMin(const OaMatrix& InA, OaF32 InMin);
