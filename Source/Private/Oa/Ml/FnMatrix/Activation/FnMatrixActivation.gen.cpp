// AUTO-GENERATED by Tools/FnAutogen/oafnautogen.py — DO NOT EDIT.
// Source of truth: Tools/FnAutogen/Schema/Ml/MlFnMatrixActivation.toml.
// Regenerate via: python3 Tools/FnAutogen/oafnautogen.py --live
// OaFnMatrix — Activation GPU operations.
// Context-based unified API: records operations to OaContext.

#include <Oa/Ml/Autograd/Nodes.h>
#include <Oa/Core/Matrix.h>
#include <Oa/Core/FnMatrix.h>
#include <Oa/Ml/FnMatrix.h>
#include <Oa/Core/Status.h>
#include <Oa/Core/Types.h>
#include <Oa/Core/BufferAccess.h>
#include <Oa/Runtime/Context.h>
#include <Oa/Runtime/Engine.h>
#include <Oa/Core/Validation.h>

#include <cassert>

static OaU32 DivCeil(OaU32 InA, OaU32 InB) { return (InA + InB - 1) / InB; }

// ─── Gelu (GELU activation: Out = GELU(A)) ────

OaMatrix OaFnMatrix::Gelu(const OaMatrix& InA) {
	auto& ctx = OaContext::GetDefault();
	OaMatrix out = OaFnMatrix::Empty(InA.Shape_, InA.Dtype_);
	OaU32 n = static_cast<OaU32>(InA.NumElements());

	struct { OaU32 Count; } push{n};
	OaBufferAccess access[] = {OaBufferAccess::Read, OaBufferAccess::Write};
	ctx.Add("Gelu", {&InA, &out}, access, &push, sizeof(push), DivCeil(n, 256));
	if (OaFnAutograd::IsEnabled() and (InA.RequiresGrad())) {
		auto _gradFn = OaMakeSharedPtr<OaGradGelu>();
		_gradFn->Saved_ = OaVec<OaMatrix>{InA};
		_gradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
		_gradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		_gradFn->OutputShape_ = out.GetShape();  // tape normalizes upstream d to this; protects elementwise bwd against viewed-shape fan-out grads
		out.MutAutograd().GradFn = _gradFn;
		out.SetRequiresGrad(true);
	}
	return out;
}

// ─── Silu (SiLU (Swish) activation: Out = A * sigmoid(A)) ────

OaMatrix OaFnMatrix::Silu(const OaMatrix& InA) {
	auto& ctx = OaContext::GetDefault();
	OaMatrix out = OaFnMatrix::Empty(InA.Shape_, InA.Dtype_);
	OaU32 n = static_cast<OaU32>(InA.NumElements());

	struct { OaU32 Count; } push{n};
	OaBufferAccess access[] = {OaBufferAccess::Read, OaBufferAccess::Write};
	ctx.Add("Silu", {&InA, &out}, access, &push, sizeof(push), DivCeil(n, 256));
	if (OaFnAutograd::IsEnabled() and (InA.RequiresGrad())) {
		auto _gradFn = OaMakeSharedPtr<OaGradSilu>();
		_gradFn->Saved_ = OaVec<OaMatrix>{InA};
		_gradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
		_gradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		_gradFn->OutputShape_ = out.GetShape();  // tape normalizes upstream d to this; protects elementwise bwd against viewed-shape fan-out grads
		out.MutAutograd().GradFn = _gradFn;
		out.SetRequiresGrad(true);
	}
	return out;
}

// ─── Relu (ReLU activation: Out = max(0, A)) ────

OaMatrix OaFnMatrix::Relu(const OaMatrix& InA) {
	auto& ctx = OaContext::GetDefault();
	OaMatrix out = OaFnMatrix::Empty(InA.Shape_, InA.Dtype_);
	OaU32 n = static_cast<OaU32>(InA.NumElements());

	struct { OaU32 Count; } push{n};
	OaBufferAccess access[] = {OaBufferAccess::Read, OaBufferAccess::Write};
	ctx.Add("Relu", {&InA, &out}, access, &push, sizeof(push), DivCeil(n, 256));
	if (OaFnAutograd::IsEnabled() and (InA.RequiresGrad())) {
		auto _gradFn = OaMakeSharedPtr<OaGradRelu>();
		_gradFn->Saved_ = OaVec<OaMatrix>{out};
		_gradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
		_gradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		_gradFn->OutputShape_ = out.GetShape();  // tape normalizes upstream d to this; protects elementwise bwd against viewed-shape fan-out grads
		out.MutAutograd().GradFn = _gradFn;
		out.SetRequiresGrad(true);
	}
	return out;
}

// ─── Tanh (Tanh activation: Out = tanh(A)) ────

OaMatrix OaFnMatrix::Tanh(const OaMatrix& InA) {
	auto& ctx = OaContext::GetDefault();
	OaMatrix out = OaFnMatrix::Empty(InA.Shape_, InA.Dtype_);
	OaU32 n = static_cast<OaU32>(InA.NumElements());

	struct { OaU32 Count; } push{n};
	OaBufferAccess access[] = {OaBufferAccess::Read, OaBufferAccess::Write};
	ctx.Add("Tanh", {&InA, &out}, access, &push, sizeof(push), DivCeil(n, 256));
	if (OaFnAutograd::IsEnabled() and (InA.RequiresGrad())) {
		auto _gradFn = OaMakeSharedPtr<OaGradTanh>();
		_gradFn->Saved_ = OaVec<OaMatrix>{out};
		_gradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
		_gradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		_gradFn->OutputShape_ = out.GetShape();  // tape normalizes upstream d to this; protects elementwise bwd against viewed-shape fan-out grads
		out.MutAutograd().GradFn = _gradFn;
		out.SetRequiresGrad(true);
	}
	return out;
}

// ─── Sigmoid (Sigmoid activation: Out = 1 / (1 + exp(-A))) ────

OaMatrix OaFnMatrix::Sigmoid(const OaMatrix& InA) {
	auto& ctx = OaContext::GetDefault();
	OaMatrix out = OaFnMatrix::Empty(InA.Shape_, InA.Dtype_);
	OaU32 n = static_cast<OaU32>(InA.NumElements());

	struct { OaU32 Count; } push{n};
	OaBufferAccess access[] = {OaBufferAccess::Read, OaBufferAccess::Write};
	ctx.Add("Sigmoid", {&InA, &out}, access, &push, sizeof(push), DivCeil(n, 256));
	if (OaFnAutograd::IsEnabled() and (InA.RequiresGrad())) {
		auto _gradFn = OaMakeSharedPtr<OaGradSigmoid>();
		_gradFn->Saved_ = OaVec<OaMatrix>{out};
		_gradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
		_gradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		_gradFn->OutputShape_ = out.GetShape();  // tape normalizes upstream d to this; protects elementwise bwd against viewed-shape fan-out grads
		out.MutAutograd().GradFn = _gradFn;
		out.SetRequiresGrad(true);
	}
	return out;
}

// ─── LeakyRelu (Leaky ReLU activation: Out = max(Alpha * A, A)) ────

OaMatrix OaFnMatrix::LeakyRelu(const OaMatrix& InA, OaF32 InAlpha) {
	auto& ctx = OaContext::GetDefault();
	OaMatrix out = OaFnMatrix::Empty(InA.Shape_, InA.Dtype_);
	OaU32 n = static_cast<OaU32>(InA.NumElements());

	struct { OaU32 Count; OaF32 Alpha; } push{n, InAlpha};
	OaBufferAccess access[] = {OaBufferAccess::Read, OaBufferAccess::Write};
	ctx.Add("LeakyRelu", {&InA, &out}, access, &push, sizeof(push), DivCeil(n, 256));
	if (OaFnAutograd::IsEnabled() and (InA.RequiresGrad())) {
		auto _gradFn = OaMakeSharedPtr<OaGradLeakyRelu>(InAlpha);
		_gradFn->Saved_ = OaVec<OaMatrix>{out};
		_gradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
		_gradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		_gradFn->OutputShape_ = out.GetShape();  // tape normalizes upstream d to this; protects elementwise bwd against viewed-shape fan-out grads
		out.MutAutograd().GradFn = _gradFn;
		out.SetRequiresGrad(true);
	}
	return out;
}

// ─── Elu (ELU activation: Out = A if A > 0 else Alpha * (exp(A) - 1)) ────

OaMatrix OaFnMatrix::Elu(const OaMatrix& InA, OaF32 InAlpha) {
	auto& ctx = OaContext::GetDefault();
	OaMatrix out = OaFnMatrix::Empty(InA.Shape_, InA.Dtype_);
	OaU32 n = static_cast<OaU32>(InA.NumElements());

	struct { OaU32 Count; OaF32 Alpha; } push{n, InAlpha};
	OaBufferAccess access[] = {OaBufferAccess::Read, OaBufferAccess::Write};
	ctx.Add("Elu", {&InA, &out}, access, &push, sizeof(push), DivCeil(n, 256));
	if (OaFnAutograd::IsEnabled() and (InA.RequiresGrad())) {
		auto _gradFn = OaMakeSharedPtr<OaGradElu>(InAlpha);
		_gradFn->Saved_ = OaVec<OaMatrix>{out};
		_gradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
		_gradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		_gradFn->OutputShape_ = out.GetShape();  // tape normalizes upstream d to this; protects elementwise bwd against viewed-shape fan-out grads
		out.MutAutograd().GradFn = _gradFn;
		out.SetRequiresGrad(true);
	}
	return out;
}

// ─── Mish (Mish activation: Out = A * tanh(softplus(A))) ────

OaMatrix OaFnMatrix::Mish(const OaMatrix& InA) {
	auto& ctx = OaContext::GetDefault();
	OaMatrix out = OaFnMatrix::Empty(InA.Shape_, InA.Dtype_);
	OaU32 n = static_cast<OaU32>(InA.NumElements());

	struct { OaU32 Count; } push{n};
	OaBufferAccess access[] = {OaBufferAccess::Read, OaBufferAccess::Write};
	ctx.Add("Mish", {&InA, &out}, access, &push, sizeof(push), DivCeil(n, 256));
	if (OaFnAutograd::IsEnabled() and (InA.RequiresGrad())) {
		auto _gradFn = OaMakeSharedPtr<OaGradMish>();
		_gradFn->Saved_ = OaVec<OaMatrix>{InA};
		_gradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
		_gradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		_gradFn->OutputShape_ = out.GetShape();  // tape normalizes upstream d to this; protects elementwise bwd against viewed-shape fan-out grads
		out.MutAutograd().GradFn = _gradFn;
		out.SetRequiresGrad(true);
	}
	return out;
}

// ─── Swiglu (SwiGLU activation: Out = SiLU(gate) * up) ────

OaMatrix OaFnMatrix::Swiglu(const OaMatrix& InA, const OaMatrix& InB) {
	auto& ctx = OaContext::GetDefault();
	OaMatrix out = OaFnMatrix::Empty(InA.Shape_, InA.Dtype_);
	OaU32 n = static_cast<OaU32>(InA.NumElements());

	struct { OaU32 Count; } push{n};
	OaBufferAccess access[] = {OaBufferAccess::Read, OaBufferAccess::Read, OaBufferAccess::Write};
	ctx.Add("Swiglu", {&InA, &InB, &out}, access, &push, sizeof(push), DivCeil(n, 256));
	if (OaFnAutograd::IsEnabled() and (InA.RequiresGrad() or InB.RequiresGrad())) {
		auto _gradFn = OaMakeSharedPtr<OaGradSwiglu>();
		_gradFn->Saved_ = OaVec<OaMatrix>{InA, InB, out};
		_gradFn->SetGraphInputs(OaVec<OaMatrix>{InA, InB});
		_gradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		_gradFn->OutputShape_ = out.GetShape();  // tape normalizes upstream d to this; protects elementwise bwd against viewed-shape fan-out grads
		out.MutAutograd().GradFn = _gradFn;
		out.SetRequiresGrad(true);
	}
	return out;
}

// ─── SiluMul (SiLU on first half, multiply with second half: Out = SiLU(A[:N

OaMatrix OaFnMatrix::SiluMul(const OaMatrix& InA, OaU32 InIntermediateSize) {
	auto& ctx = OaContext::GetDefault();
	OaMatrixShape outShape = InA.Shape_;
	assert(outShape.Rank > 0 and InA.Size(-1) % 2 == 0 and static_cast<OaI64>(InIntermediateSize) == InA.Size(-1) / 2);
	outShape.Dims[static_cast<OaUsize>(outShape.Rank - 1)] = InA.Size(-1) / 2;
	OaMatrix out = OaFnMatrix::Empty(outShape, InA.Dtype_);
	OaU32 n = static_cast<OaU32>(out.NumElements());

	struct { OaU32 Count; OaU32 intermediate_size; } push{n, InIntermediateSize};
	OaBufferAccess access[] = {OaBufferAccess::Read, OaBufferAccess::Write};
	ctx.Add("SiluMul", {&InA, &out}, access, &push, sizeof(push), DivCeil(static_cast<OaU32>(out.NumElements()), 256));
	if (OaFnAutograd::IsEnabled() and (InA.RequiresGrad())) {
		auto _gradFn = OaMakeSharedPtr<OaGradSiluMul>();
		_gradFn->Saved_ = OaVec<OaMatrix>{InA};
		_gradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
		_gradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		_gradFn->OutputShape_ = out.GetShape();  // tape normalizes upstream d to this; protects elementwise bwd against viewed-shape fan-out grads
		out.MutAutograd().GradFn = _gradFn;
		out.SetRequiresGrad(true);
	}
	return out;
}

// ─── Geglu (GEGLU activation: Out = A[:N] * GELU(A[N:])) ────

OaMatrix OaFnMatrix::Geglu(const OaMatrix& InA, OaU32 InIntermediateSize) {
	auto& ctx = OaContext::GetDefault();
	OaMatrixShape outShape = InA.Shape_;
	assert(outShape.Rank > 0 and InA.Size(-1) % 2 == 0 and static_cast<OaI64>(InIntermediateSize) == InA.Size(-1) / 2);
	outShape.Dims[static_cast<OaUsize>(outShape.Rank - 1)] = InA.Size(-1) / 2;
	OaMatrix out = OaFnMatrix::Empty(outShape, InA.Dtype_);
	OaU32 n = static_cast<OaU32>(out.NumElements());

	struct { OaU32 Count; OaU32 intermediate_size; } push{n, InIntermediateSize};
	OaBufferAccess access[] = {OaBufferAccess::Read, OaBufferAccess::Write};
	ctx.Add("Geglu", {&InA, &out}, access, &push, sizeof(push), DivCeil(static_cast<OaU32>(out.NumElements()), 256));
	if (OaFnAutograd::IsEnabled() and (InA.RequiresGrad())) {
		auto _gradFn = OaMakeSharedPtr<OaGradGeglu>();
		_gradFn->Saved_ = OaVec<OaMatrix>{InA};
		_gradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
		_gradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		_gradFn->OutputShape_ = out.GetShape();  // tape normalizes upstream d to this; protects elementwise bwd against viewed-shape fan-out grads
		out.MutAutograd().GradFn = _gradFn;
		out.SetRequiresGrad(true);
	}
	return out;
}

// ─── Softplus (Softplus: Out = log(1+exp(A)), computed stably as max(A,0)+lo

OaMatrix OaFnMatrix::Softplus(const OaMatrix& InA) {
	auto& ctx = OaContext::GetDefault();
	OaMatrix out = OaFnMatrix::Empty(InA.Shape_, InA.Dtype_);
	OaU32 n = static_cast<OaU32>(InA.NumElements());

	struct { OaU32 Count; } push{n};
	OaBufferAccess access[] = {OaBufferAccess::Read, OaBufferAccess::Write};
	ctx.Add("Softplus", {&InA, &out}, access, &push, sizeof(push), DivCeil(n, 256));
	if (OaFnAutograd::IsEnabled() and (InA.RequiresGrad())) {
		auto _gradFn = OaMakeSharedPtr<OaGradSoftplus>();
		_gradFn->Saved_ = OaVec<OaMatrix>{out};
		_gradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
		_gradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		_gradFn->OutputShape_ = out.GetShape();  // tape normalizes upstream d to this; protects elementwise bwd against viewed-shape fan-out grads
		out.MutAutograd().GradFn = _gradFn;
		out.SetRequiresGrad(true);
	}
	return out;
}
