// AUTO-GENERATED by Tools/FnAutogen/oafnautogen.py — DO NOT EDIT.
// Source of truth: Tools/FnAutogen/Schema/Ml/MlFnMatrixActivation.toml.
// Regenerate via: python3 Tools/FnAutogen/oafnautogen.py --live

// OaFnMatrix — Activation overloads.

	/// Gelu: GELU activation: Out = GELU(A).
	/// Context-based: auto y = OaFnMatrix::Gelu(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Gelu(const OaMatrix& InA);

	/// Silu: SiLU (Swish) activation: Out = A * sigmoid(A).
	/// Context-based: auto y = OaFnMatrix::Silu(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Silu(const OaMatrix& InA);

	/// Relu: ReLU activation: Out = max(0, A).
	/// Context-based: auto y = OaFnMatrix::Relu(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Relu(const OaMatrix& InA);

	/// Tanh: Tanh activation: Out = tanh(A).
	/// Context-based: auto y = OaFnMatrix::Tanh(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Tanh(const OaMatrix& InA);

	/// Sigmoid: Sigmoid activation: Out = 1 / (1 + exp(-A)).
	/// Context-based: auto y = OaFnMatrix::Sigmoid(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Sigmoid(const OaMatrix& InA);

	/// LeakyRelu: Leaky ReLU activation: Out = max(Alpha * A, A).
	/// Context-based: auto y = OaFnMatrix::LeakyRelu(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix LeakyRelu(const OaMatrix& InA, OaF32 InAlpha);

	/// Elu: ELU activation: Out = A if A > 0 else Alpha * (exp(A) - 1).
	/// Context-based: auto y = OaFnMatrix::Elu(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Elu(const OaMatrix& InA, OaF32 InAlpha);

	/// Mish: Mish activation: Out = A * tanh(softplus(A)).
	/// Context-based: auto y = OaFnMatrix::Mish(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Mish(const OaMatrix& InA);

	/// Swiglu: SwiGLU activation: Out = SiLU(gate) * up.
	/// Context-based: auto y = OaFnMatrix::Swiglu(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Swiglu(const OaMatrix& InA, const OaMatrix& InB);

	/// SiluMul: SiLU on first half, multiply with second half: Out = SiLU(A[:N]) * A[N:].
	/// Context-based: auto y = OaFnMatrix::SiluMul(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix SiluMul(const OaMatrix& InA, OaU32 InIntermediateSize);

	/// Geglu: GEGLU activation: Out = A[:N] * GELU(A[N:]).
	/// Context-based: auto y = OaFnMatrix::Geglu(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Geglu(const OaMatrix& InA, OaU32 InIntermediateSize);

	/// Softplus: Softplus: Out = log(1+exp(A)), computed stably as max(A,0)+log1p(exp(-|A|)) (no f32 overflow).
	/// Context-based: auto y = OaFnMatrix::Softplus(...);
	/// Records operation to thread-local default context.
	[[nodiscard]] OaMatrix Softplus(const OaMatrix& InA);
