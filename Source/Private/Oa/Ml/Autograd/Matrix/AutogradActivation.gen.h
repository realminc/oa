// AUTO-GENERATED by Tools/FnAutogen/oafnautogen.py — DO NOT EDIT.
// Source of truth: Tools/FnAutogen/Schema/Ml/MlFnMatrixActivation.toml.
// Output: Source/Private/Oa/Ml/Autograd/Matrix/AutogradActivation.gen.h
// Regenerate via: python3 Tools/FnAutogen/oafnautogen.py --live

#pragma once

#include <Oa/Ml/Autograd.h>
#include <Oa/Core/FnMatrix.h>
#include <Oa/Ml/FnMatrix.h>

// ─── Activation gradient nodes (autogenerated) ─────────────────────────────

class OaGradGelu final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::GeluBwd(Saved_[0], InDOut);
	}
};

class OaGradSilu final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::SiluBwd(Saved_[0], InDOut);
	}
};

class OaGradRelu final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::ReluBwd(Saved_[0], InDOut);
	}
};

class OaGradTanh final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::TanhBwd(Saved_[0], InDOut);
	}
};

class OaGradSigmoid final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::SigmoidBwd(Saved_[0], InDOut);
	}
};

class OaGradLeakyRelu final : public OaGradNode {
public:
	explicit OaGradLeakyRelu(OaF32 InAlpha) noexcept : Alpha_(InAlpha) {}
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::LeakyReluBwd(Saved_[0], InDOut, Alpha_);
	}
	OaF32 Alpha_;
};

class OaGradElu final : public OaGradNode {
public:
	explicit OaGradElu(OaF32 InAlpha) noexcept : Alpha_(InAlpha) {}
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::EluBwd(Saved_[0], InDOut, Alpha_);
	}
	OaF32 Alpha_;
};

class OaGradMish final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::MishBwd(Saved_[0], InDOut);
	}
};

class OaGradSoftplus final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::SoftplusBwd(Saved_[0], InDOut);
	}
};

