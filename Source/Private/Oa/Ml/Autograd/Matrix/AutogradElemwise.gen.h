// AUTO-GENERATED by Tools/FnAutogen/oafnautogen.py — DO NOT EDIT.
// Source of truth: Tools/FnAutogen/Schema/Core/CoreFnMatrixElemwise.toml.
// Output: Source/Private/Oa/Ml/Autograd/Matrix/AutogradElemwise.gen.h
// Regenerate via: python3 Tools/FnAutogen/oafnautogen.py --live

#pragma once

#include <Oa/Ml/Autograd.h>
#include <Oa/Core/FnMatrix.h>
#include <Oa/Ml/FnMatrix.h>

// ─── Elemwise gradient nodes (autogenerated) ─────────────────────────────

class OaGradAdd final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = InDOut;
		if (OutDIn.Size() > 1) OutDIn[1] = InDOut;
	}
};

class OaGradSub final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = InDOut;
		if (OutDIn.Size() > 1) OutDIn[1] = OaFnMatrix::Scale(InDOut, -1.0f);
	}
};

class OaGradMul final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		const OaMatrix& a = Saved_[0];
		const OaMatrix& b = Saved_[1];
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::Mul(InDOut, b);
		if (OutDIn.Size() > 1) OutDIn[1] = OaFnMatrix::Mul(InDOut, a);
	}
};

class OaGradScale final : public OaGradNode {
public:
	explicit OaGradScale(OaF32 InScalar) noexcept : Scalar_(InScalar) {}
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::Scale(InDOut, Scalar_);
	}
	OaF32 Scalar_;
};

class OaGradLog final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::Div(InDOut, Saved_[0]);
	}
};

class OaGradSqrt final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::Div(InDOut, OaFnMatrix::Scale(Saved_[0], 2.0f));
	}
};

class OaGradPow final : public OaGradNode {
public:
	explicit OaGradPow(OaF32 InExponent) noexcept : Exponent_(InExponent) {}
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) {
			const OaMatrix& x = Saved_[0];
			auto dPow = OaFnMatrix::Pow(x, Exponent_ - 1.0f);
			OutDIn[0] = OaFnMatrix::Scale(OaFnMatrix::Mul(InDOut, dPow), Exponent_);
		}
	}
	OaF32 Exponent_;
};

class OaGradAddScalar final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = InDOut;
	}
};

class OaGradSubScalar final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = InDOut;
	}
};

class OaGradDivScalar final : public OaGradNode {
public:
	explicit OaGradDivScalar(OaF32 InScalar) noexcept : Scalar_(InScalar) {}
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::Scale(InDOut, 1.0f / Scalar_);
	}
	OaF32 Scalar_;
};

class OaGradExp final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::Mul(InDOut, Saved_[0]);
	}
};

class OaGradSin final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::Mul(InDOut, OaFnMatrix::Cos(Saved_[0]));
	}
};

class OaGradCos final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) OutDIn[0] = OaFnMatrix::Scale(OaFnMatrix::Mul(InDOut, OaFnMatrix::Sin(Saved_[0])), -1.0f);
	}
};

class OaGradReciprocal final : public OaGradNode {
public:
	void Backward(const OaMatrix& InDOut, OaVec<OaMatrix>& OutDIn) override {
		if (OutDIn.Size() > 0) {
			const OaMatrix& out = Saved_[0];
			OutDIn[0] = OaFnMatrix::Scale(OaFnMatrix::Mul(InDOut, OaFnMatrix::Mul(out, out)), -1.0f);
		}
	}
};

