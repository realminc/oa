// AUTO-GENERATED by Tools/FnAutogen/oafnautogen.py — DO NOT EDIT.
// Source of truth: Tools/FnAutogen/Schema/**.toml [ops.autograd].
// Attaches schema-owned differentiation policy to hand-written lowerings.
// Include only after <Oa/Ml/Autograd.h> has defined the gradient nodes.
#pragma once

namespace OaGeneratedAutogradAttach {

namespace OaFnLoss {

inline OaStatus CrossEntropy(OaMatrix& Out, const OaMatrix& InLogits, const OaMatrix& InTargets, OaSemanticOperationId InSemanticOperation) {
	if (not (OaFnAutograd::IsEnabled() and (InLogits.RequiresGrad()))) return OaStatus::Ok();
	auto GradFn = OaMakeSharedPtr<OaGradCrossEntropy>();
	GradFn->Saved_ = OaVec<OaMatrix>{InLogits, InTargets};
	GradFn->SetGraphInputs(OaVec<OaMatrix>{InLogits});
	GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
	GradFn->OutputShape_ = Out.GetShape();
	OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
	Out.MutAutograd().GradFn = GradFn;
	return OaStatus::Ok();
}

} // namespace OaFnLoss

namespace OaFnMatrix {

inline OaStatus Add(OaMatrix& Out, const OaMatrix& InA, const OaMatrix& InB, OaSemanticOperationId InSemanticOperation) {
	if (not (OaFnAutograd::IsEnabled() and (InA.RequiresGrad() or InB.RequiresGrad()))) return OaStatus::Ok();
	if (InA.GetShape() != InB.GetShape()) {
		auto GradFn = OaMakeSharedPtr<OaGradBcastBinary>();
		GradFn->Op_ = OaBcastBinOp::Add;
		GradFn->Saved_ = OaVec<OaMatrix>{InA, InB};
		GradFn->SetGraphInputs(OaVec<OaMatrix>{InA, InB});
		GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		GradFn->OutputShape_ = Out.GetShape();
		OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
		Out.MutAutograd().GradFn = GradFn;
		return OaStatus::Ok();
	}
	auto GradFn = OaMakeSharedPtr<OaGradAdd>();
	GradFn->Saved_ = OaVec<OaMatrix>{};
	GradFn->SetGraphInputs(OaVec<OaMatrix>{InA, InB});
	GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
	OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
	Out.MutAutograd().GradFn = GradFn;
	return OaStatus::Ok();
}

inline OaStatus Div(OaMatrix& Out, const OaMatrix& InA, const OaMatrix& InB, OaSemanticOperationId InSemanticOperation) {
	if (not (OaFnAutograd::IsEnabled() and (InA.RequiresGrad() or InB.RequiresGrad()))) return OaStatus::Ok();
	if (InA.GetShape() != InB.GetShape()) {
		auto GradFn = OaMakeSharedPtr<OaGradBcastBinary>();
		GradFn->Op_ = OaBcastBinOp::Div;
		GradFn->Saved_ = OaVec<OaMatrix>{InA, InB};
		GradFn->SetGraphInputs(OaVec<OaMatrix>{InA, InB});
		GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		GradFn->OutputShape_ = Out.GetShape();
		OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
		Out.MutAutograd().GradFn = GradFn;
		return OaStatus::Ok();
	}
	auto GradFn = OaMakeSharedPtr<OaGradDiv>();
	GradFn->Saved_ = OaVec<OaMatrix>{InA, InB};
	GradFn->SetGraphInputs(OaVec<OaMatrix>{InA, InB});
	GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
	OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
	Out.MutAutograd().GradFn = GradFn;
	return OaStatus::Ok();
}

inline OaStatus LayerNorm(OaMatrix& Out, const OaMatrix& InSelf, const OaMatrix& InWeight, const OaMatrix& InBias, OaF32 InEps, OaSemanticOperationId InSemanticOperation) {
	if (not (OaFnAutograd::IsEnabled() and (InSelf.RequiresGrad() or InWeight.RequiresGrad() or InBias.RequiresGrad()))) return OaStatus::Ok();
	auto GradFn = OaMakeSharedPtr<OaGradLayerNorm>();
	GradFn->Saved_ = OaVec<OaMatrix>{InSelf, InWeight, InBias, Out};
	GradFn->SetGraphInputs(OaVec<OaMatrix>{InSelf, InWeight, InBias});
	GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
	GradFn->Eps_ = InEps;
	GradFn->OutputShape_ = Out.GetShape();
	OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
	Out.MutAutograd().GradFn = GradFn;
	return OaStatus::Ok();
}

inline OaStatus LogSoftmax(OaMatrix& Out, const OaMatrix& InA, OaI32 InDim, OaSemanticOperationId InSemanticOperation) {
	if (not (OaFnAutograd::IsEnabled() and (InA.RequiresGrad()))) return OaStatus::Ok();
	auto GradFn = OaMakeSharedPtr<OaGradLogSoftmax>();
	GradFn->Saved_ = OaVec<OaMatrix>{Out};
	GradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
	GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
	GradFn->Dim_ = InDim;
	GradFn->OutputShape_ = Out.GetShape();
	OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
	Out.MutAutograd().GradFn = GradFn;
	return OaStatus::Ok();
}

inline OaStatus MatMulNt(OaMatrix& Out, const OaMatrix& InA, const OaMatrix& InB, OaSemanticOperationId InSemanticOperation) {
	if (not (OaFnAutograd::IsEnabled() and (InA.RequiresGrad() or InB.RequiresGrad()) and InA.Rank() == 2 and InB.Rank() == 2)) return OaStatus::Ok();
	auto GradFn = OaMakeSharedPtr<OaGradMatMulNt>();
	GradFn->Saved_ = OaVec<OaMatrix>{InA, InB};
	GradFn->SetGraphInputs(OaVec<OaMatrix>{InA, InB});
	GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
	GradFn->OutputShape_ = Out.GetShape();
	OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
	Out.MutAutograd().GradFn = GradFn;
	return OaStatus::Ok();
}

inline OaStatus Max(OaMatrix& Out, const OaMatrix& InA, OaSemanticOperationId InSemanticOperation) {
	if (not (OaFnAutograd::IsEnabled() and (InA.RequiresGrad()))) return OaStatus::Ok();
	auto GradFn = OaMakeSharedPtr<OaGradMax>();
	GradFn->Saved_ = OaVec<OaMatrix>{InA, Out};
	GradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
	GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
	GradFn->OutputShape_ = Out.GetShape();
	OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
	Out.MutAutograd().GradFn = GradFn;
	return OaStatus::Ok();
}

inline OaStatus Mean(OaMatrix& Out, const OaMatrix& InA, OaI32 InDim, OaSemanticOperationId InSemanticOperation) {
	if (not (OaFnAutograd::IsEnabled() and (InA.RequiresGrad()))) return OaStatus::Ok();
	auto GradFn = OaMakeSharedPtr<OaGradMean>();
	GradFn->Saved_ = OaVec<OaMatrix>{InA};
	GradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
	GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
	GradFn->Dim_ = InDim;
	GradFn->OutputShape_ = Out.GetShape();
	OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
	Out.MutAutograd().GradFn = GradFn;
	return OaStatus::Ok();
}

inline OaStatus Mul(OaMatrix& Out, const OaMatrix& InA, const OaMatrix& InB, OaSemanticOperationId InSemanticOperation) {
	if (not (OaFnAutograd::IsEnabled() and (InA.RequiresGrad() or InB.RequiresGrad()))) return OaStatus::Ok();
	if (InA.GetShape() != InB.GetShape()) {
		auto GradFn = OaMakeSharedPtr<OaGradBcastBinary>();
		GradFn->Op_ = OaBcastBinOp::Mul;
		GradFn->Saved_ = OaVec<OaMatrix>{InA, InB};
		GradFn->SetGraphInputs(OaVec<OaMatrix>{InA, InB});
		GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		GradFn->OutputShape_ = Out.GetShape();
		OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
		Out.MutAutograd().GradFn = GradFn;
		return OaStatus::Ok();
	}
	auto GradFn = OaMakeSharedPtr<OaGradMul>();
	GradFn->Saved_ = OaVec<OaMatrix>{InA, InB};
	GradFn->SetGraphInputs(OaVec<OaMatrix>{InA, InB});
	GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
	GradFn->OutputShape_ = Out.GetShape();
	OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
	Out.MutAutograd().GradFn = GradFn;
	return OaStatus::Ok();
}

inline OaStatus RmsNorm(OaMatrix& Out, const OaMatrix& InSelf, const OaMatrix& InWeight, OaF32 InEps, OaSemanticOperationId InSemanticOperation) {
	if (not (OaFnAutograd::IsEnabled() and (InSelf.RequiresGrad() or InWeight.RequiresGrad()))) return OaStatus::Ok();
	auto GradFn = OaMakeSharedPtr<OaGradRmsNorm>();
	GradFn->Saved_ = OaVec<OaMatrix>{InSelf, InWeight, Out, Out};
	GradFn->SetGraphInputs(OaVec<OaMatrix>{InSelf, InWeight});
	GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
	GradFn->Eps_ = InEps;
	GradFn->OutputShape_ = Out.GetShape();
	OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
	Out.MutAutograd().GradFn = GradFn;
	return OaStatus::Ok();
}

inline OaStatus Softmax(OaMatrix& Out, const OaMatrix& InA, OaI32 InDim, OaSemanticOperationId InSemanticOperation) {
	if (not (OaFnAutograd::IsEnabled() and (InA.RequiresGrad()))) return OaStatus::Ok();
	auto GradFn = OaMakeSharedPtr<OaGradSoftmax>();
	GradFn->Saved_ = OaVec<OaMatrix>{Out};
	GradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
	GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
	GradFn->Dim_ = InDim;
	GradFn->OutputShape_ = Out.GetShape();
	OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
	Out.MutAutograd().GradFn = GradFn;
	return OaStatus::Ok();
}

inline OaStatus Sub(OaMatrix& Out, const OaMatrix& InA, const OaMatrix& InB, OaSemanticOperationId InSemanticOperation) {
	if (not (OaFnAutograd::IsEnabled() and (InA.RequiresGrad() or InB.RequiresGrad()))) return OaStatus::Ok();
	if (InA.GetShape() != InB.GetShape()) {
		auto GradFn = OaMakeSharedPtr<OaGradBcastBinary>();
		GradFn->Op_ = OaBcastBinOp::Sub;
		GradFn->Saved_ = OaVec<OaMatrix>{InA, InB};
		GradFn->SetGraphInputs(OaVec<OaMatrix>{InA, InB});
		GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
		GradFn->OutputShape_ = Out.GetShape();
		OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
		Out.MutAutograd().GradFn = GradFn;
		return OaStatus::Ok();
	}
	auto GradFn = OaMakeSharedPtr<OaGradSub>();
	GradFn->Saved_ = OaVec<OaMatrix>{};
	GradFn->SetGraphInputs(OaVec<OaMatrix>{InA, InB});
	GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
	OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
	Out.MutAutograd().GradFn = GradFn;
	return OaStatus::Ok();
}

inline OaStatus Sum(OaMatrix& Out, const OaMatrix& InA, OaSemanticOperationId InSemanticOperation) {
	if (not (OaFnAutograd::IsEnabled() and (InA.RequiresGrad()))) return OaStatus::Ok();
	auto GradFn = OaMakeSharedPtr<OaGradSum>();
	GradFn->Saved_ = OaVec<OaMatrix>{InA};
	GradFn->SetGraphInputs(OaVec<OaMatrix>{InA});
	GradFn->SequenceNr_ = OaFnAutograd::NextSeq();
	GradFn->OutputShape_ = Out.GetShape();
	OA_RETURN_IF_ERROR(OaFnAutograd::AttachSemantic(GradFn, InSemanticOperation));
	Out.MutAutograd().GradFn = GradFn;
	return OaStatus::Ok();
}

} // namespace OaFnMatrix

} // namespace OaGeneratedAutogradAttach
